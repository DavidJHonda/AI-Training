Create a video overview of the attached lesson "Tokens," about 3 minutes long. The attached tokens.md is the full lesson text; the numbered images are the lesson's key boxes, in order — show each one as the narration reaches it. Focus: how your plain-English words become the numbers a computer can work with. Open on the gap: chatting with AI feels like plain English both ways, but computers work only with numbers — they don't read text at all, so your text must be converted before the AI can read it. The obvious solution, one number per word, falls apart fast: names, slang, typos, and code would need millions of numbers and still miss words nobody has invented yet. The smarter solution is reusable chunks — image 1: the vocabulary stores UN once and reuses that one piece across thousands of words. Name the process tokenization and the chunks tokens; a token can be a whole word, part of a word, punctuation, an emoji, or even the space before a word, and the splitting is done by a small ordinary program before your words ever reach the model. Vocabularies are large and fixed: ChatGPT's holds about 200,000 tokens and Gemini's about 256,000. Each token gets a number, its token ID — an address in the vocabulary that says which token, not what it means — image 2: a human sees the word cat and instantly knows soft fur and whiskers; AI gets 9246, a number, not meaning yet. Then image 3, the real tokenization examples: walk all five in order — unbelievable, basketball, ChatGPT, I-heart-AI, and the long URL — each splitting into known pieces with their token IDs. End on the attached close image — the final thing on screen.
Rules:
1. No photographic or stock imagery of any kind — no Getty or watermarked images.
2. Never show the lesson document itself as a page on screen; show the attached images or draw fresh visuals.
3. Show the attached images exactly as provided — never redraw, restyle, or replace them with your own version.
4. Never place white or light-colored text on a light background; all text and labels must be dark ink on light backgrounds.
5. Do not invent numbers, percentages, or statistics — use only values that appear in the lesson.
6. The narrator never asks the viewer to pause, guess, or answer; answer any question the lesson poses in the same breath.
7. The close image is the final frame of the video — nothing plays after it, no outro or summary scene.