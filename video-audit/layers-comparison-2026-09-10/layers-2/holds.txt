# layers-2: spans of >= 6s with no scene cut, and the
# narration underneath. Free under the dead-time rule while the
# narration is still walking what is on screen; charged once it
# has moved past it. Read both columns before deducting.

== HOLD 0:09.10 -> 0:16.17  (7.1s)
   narration: Take a look at this sentence. The horse raced past the barn fell. On the first read, it feels broken. So you read it again, testing different interpretations.

== HOLD 0:27.03 -> 0:40.00  (13.0s)
   narration: Human comprehension relies on updating meaning incrementally, refining the context until the pieces fit. Artificial intelligence does something mathematically similar, using a sequence of operations called layers.

== HOLD 0:40.00 -> 0:51.10  (11.1s)
   narration: This neural network passes data through a stack of sequential layers to process text. Inside each layer, attention and transformation calculate how your words relate. The AI represents words as a starting row of decimal numbers.

== HOLD 0:59.87 -> 1:11.47  (11.6s)
   narration: Across the columns, notice how these decimal values shift and change significantly layer by layer. These shifting decimals are the machine equivalent of rereading a text. The AI is incrementally updating the contextual meaning of the words at each mathematical step. Let's trace an example on this board.

== HOLD 1:17.43 -> 1:24.83  (7.4s)
   narration: Focus on the ambiguous word, it. Its starting numbers don't reveal the pronouns reference, but moving through layers 1 and 2, the numbers update, capturing specific mathematical connections to cat,

== HOLD 1:25.60 -> 1:33.17  (7.6s)
   narration: but moving through layers 1 and 2, the numbers update, capturing specific mathematical connections to cat, building over successive layers. Pause the video for a moment.

== HOLD 1:34.93 -> 1:41.90  (7.0s)
   narration: Pause the video for a moment. If the AI is trying to link these words, what must happen to those decimals for the sentence to finally make sense?

== HOLD 1:41.90 -> 1:59.90  (18.0s)
   narration: If the AI is trying to link these words, what must happen to those decimals for the sentence to finally make sense? Here is the result. By this final stage, the decimals for the word it have been transformed to align with the mathematical patterns associated with the word cat. Because those numerical values now match, the AI interprets the pronoun as the specific animal from earlier in the sentence. Real-world AI models operate at an enormous scale.

== HOLD 1:59.90 -> 2:22.40  (22.5s)
   narration: Real-world AI models operate at an enormous scale. Companies deploy dozens, sometimes over 100 sequential layers to process complex language. If a simple horse sentence takes three reads, imagine untangling sarcasm, story twists, or complicated logic. AI requires massive layer depth to compute these relationships. So why can't developers just add infinite layers to make the AI perfectly understand everything?

== HOLD 2:22.40 -> 2:42.00  (19.6s)
   narration: So why can't developers just add infinite layers to make the AI perfectly understand everything? Because we hit strict physical limitations. Processing data through more layers requires exponentially more computing power and processing time. The extra benefit and comprehension always has to be worth that heavy physical cost. Meaning builds up layer by layer.
