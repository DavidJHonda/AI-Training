# Be Smarter Than the Tool — Project Briefing

## What this is
An instructor-led AI education web app for high schoolers, built to be taught live but easily repurposed for self-paced use. Single HTML file (index.html) with inline React. Deployed via Vercel from GitHub repo "AI-Training". Owner: David. Audience: his twins and other 16-year-old students.

## Lesson map
62 lessons across 8 section groups (a six-part content spine plus Start Smarter and Finish Smarter), listed in delivery order. Each lesson is listed as `Displayed Label (internal id)`. The id is the React component key and navigation reference used in code; the label is what students see in the UI.

- Start Smarter (8): Welcome (welcome), Why Learn AI? (whydeeper), What Is AI? (aihistory), Meet the Tool (llms), Does AI Think? (doesaithink), Does School Matter? (whybother), Study with AI (studying), What You Can Control (control)
- Understand AI (13): one section group with a single opener; the former Inside AI and AI Answers sub-openers were removed and their framing folded into tokens (top) and training (end):
  - Foundations: Opener (openerfoundations), How We Got Here (howwegothere), Rules vs Patterns (aivscode), Harder to Control (norules), AI is Math (aiismath)
  - Inside the model: Tokens (tokens), Embeddings: Meaning as Numbers (embeddings), Attention (attention), Layers (layers — now also absorbs the former Vector Space lesson as its closing section), Training (training); Black Box (insidethemodel) closes the whole Understand AI group (see 2026-06-17 notes below)
  - How AI builds an answer: Context Window (prompt), Inference: From Prompt to Output (inference). The former Patterns lesson (patterns) was removed 2026-06-03 as redundant: its one unique idea (training writes patterns into the weights, inference reads them back) now lives in the What Is AI? lesson (aihistory), and the term "weights" now debuts in Training. The standalone Probability lesson (probability) was removed 2026-06-04 and its prediction activities (bar chart + Guess the Most Probable Word) moved to What's an LLM? (llms). A standalone AI Primer lesson (primer) was added then dissolved the same day (2026-06-04): its conceptual loop (patterns → training → probability → prediction) relocated to What Is AI? as prose; its prediction pieces moved to What's an LLM?; the everyday-AI breadth (Spotify/Netflix/Maps showcase, Google Maps example) moved to the new You Already Use AI lesson (youalreadyuse); the generative-AI intro and Stored Answers vs Built From Probability SEE IT moved to What's an LLM?. Understand AI dropped from 16 to 15. Cut copy is logged in docs/parking-lot.md. Update (2026-06-04): the bar-chart worked example ("How the model picks the next word") was reworked around a peanut-butter example and moved to What Is AI? (aihistory) as the payoff to a single PB&J throughline running through patterns/training/probability/prediction; the Guess the Most Probable Word activity remains in What's an LLM? (llms). Update (2026-06-05): the everyday-AI apps showcase ("AI is hiding in apps you use every day") moved from You Already Use AI to Why Learn AI? (whydeeper), and You Already Use AI (youalreadyuse) was then removed entirely; its component stays defined in the file as dead code and is gone from SECTION_GROUPS/SECTION_META/SECTION_COMPONENTS. Start Smarter dropped from 8 to 7. Update (2026-06-05): What's an LLM? (llms) was rewritten and retitled "Meet the Tool" (kicker "WHAT YOU'RE ACTUALLY USING"), reframed around two kinds of AI (non-generative sorting/recommending vs generative content creation) and the AI/LLM naming via a car/engine analogy; it keeps the "LLM, decoded" and "Stored Answers vs. Built From Probability" SEE ITs. The specific products/models block (ChatGPT/Claude/Gemini + Opus/Sonnet/Haiku) was cut as redundant with Choosing the Product (modelselection) and Choosing the Model (choosemodel), the Guess the Most Probable Word try-it was removed (prediction now lives in What Is AI?), and the whats-an-llm.jpg illustration was deleted. Update (2026-06-06): the "Stored Answers vs. Built From Probability" SEE IT moved out of Meet the Tool (llms) to Rules vs Patterns (aivscode), placed as a hook right after the recipe/chef analogy and before the first rules-vs-patterns ComparePanel pair (localStorage keys renamed seeit-llms-twoSystems* → seeit-aivscode-twoSystems*); in its place Meet the Tool gained a new "Two kinds of AI, one task" SEE IT (component WatchTonightSeeIt, with a reusable TypeOut word-by-word typing helper). For the task "What should I watch tonight?", Predictive AI serves a Netflix-style ranked list (top pick badged) while Generative AI types out the same first suggestion; the student then clicks "I already saw that at a friend's" and the two diverge: Predictive slides the highlight to the next title on its list (the first struck through and tagged "seen") while Generative shows a user message and types an adapted, reasoned pick, a back-and-forth that makes generative AI's value concrete. State persists via seeit-llms-watchStep.
- Work With AI (9): Opener (openerworkwith), What AI Does Best (whatitdoesbest), Choose the App (modelselection), Choose the Model (choosemodel), Effort Dial (effort), Customization & Memory (customization), Questions Matter (questionsvaluable), Art of Prompting (prompting), Thought Partner (thoughtpartner)
- Check the Output (5): Opener (openercheck), How Much to Check (howmuchtocheck), Critical Thinking (critical), Verify (verify), Evaluate (evaluating)
- Avoid Traps (9): Opener (openerprotect), Hallucination (hallucination), Training Bias Trap (trainingbias), Document Trap (documenttrap), Mind Trap (mindtrap), Flattery Trap (flattery), Engagement Trap (engagementtrap), Support Trap (supporttrap), The Fake Trap (faketrap)
- Embrace the Future (5): Opener (openerrealworld), Work Changes (workchanges), The Rise of Agents (agents), When AI Judges You (aijudges), The Future (aifuture)
- Build Your Skills (7): Opener (openerskills), Integrity (integrity), Privacy (privacy), Skills That Matter (humanedge), Creative Thinking (creativethinking), Be Curious (becurious), Your Edge (buildedge)

Update (2026-06-10): When Not to Use AI (whennot) was removed and merged into How Much to Check (howmuchtocheck), which simultaneously moved from Protect Yourself into Check the Output as that section's first lesson after the opener ("set the dial" before the checking skills). The merged lesson gained a "SOMETIMES THE ANSWER IS NO" beat (the four don't-reach-for-it categories, moved verbatim) and a seventh Make the Call scenario ("Send it for me?" / Acting for you); whennot's KeyInsight (the say-it-out-loud test) was folded into Academic Integrity's KeyInsight body; its two scenarios that duplicated Make the Call rows and the "A friend's secret" privacy scenario were cut (logged in docs/parking-lot.md). Gates rewired: openercheck → howmuchtocheck → critical, supporttrap → integrity. The Check the Output opener gained a third overview group (SET THE DIAL, first). WhenNotSection and WHENNOT_SCENARIOS remain in the file as dead code. Check the Output 7 → 8, Protect Yourself 10 → 8, course total 62 → 61.

Update (2026-06-15): Mess to Meaning (data) was merged into Rules vs Patterns (aivscode), since the rules-vs-patterns contrast and the mess payoff are one idea. The merged lesson keeps both CompareBoxes, the PS5 SEE IT, and both TRY ITs (Which One Handles This?, then What Could AI Pull Out?), with the Next gate now requiring both activities (fitAllAnswered && fuelAllAnswered); data's "AI runs on patterns" opener was replaced by a bridge paragraph back to the first TRY IT, and its "The move, step by step" ShowcaseBox was cut (parked in docs/parking-lot.md). Harder to Control (norules) stays standalone and now gates to AI is Math (aiismath). DataSection stays in the file as a dead stub (returns null), removed from SECTION_GROUPS/SECTION_META/SECTION_COMPONENTS; the Unstructured Data glossary term repoints from data to aivscode. Understand AI 15 → 14, Foundations 6 → 5, course total 63 → 62. (While updating these counts, two pre-existing lesson-map drifts were reconciled against SECTION_GROUPS: Choose the Model (choosemodel) had been re-split into its own standalone lesson in Work With AI after the 2026-06-08 merge note, and Be Curious (becurious) had been added to Build Your Skills; neither was logged, so the map and total had read 2 low. The live app counts 62.) Update (2026-06-16): Rules vs Patterns (aivscode) was re-pitched as one idea rather than two — a single cause→effect arc (AI runs on patterns, not rules, which is *why* it makes sense of messy/unfamiliar input). Its two CompareBoxes were merged into one taller box (each column now flows what-it-needs → so-how-it-acts → superpower, folding the structured-table-vs-modalities visual into the "what it needs" row), and its two TRY ITs collapsed into one fused "Which One Handles This?" activity: the rule-vs-pattern discrimination stays the learner's choice, while the pattern-win feedback now reveals what AI pulls out of the mess (voice memo → to-dos; grocery photo → checklist plus the "can't invent" guardrail). The scam-text row was swapped for the grocery-photo row, and the standalone "What Could AI Pull Out?" TRY IT plus two bridge paragraphs were cut (parked in docs/parking-lot.md). SEE IT now precedes the merged box (experience before systematize); the Next gate drops to a single flag (fitAllAnswered), and the "It restructures; it doesn't invent" KeyInsight survives, now triggered by the one activity. Lesson count and course total unchanged (62).

Update (2026-06-15): AI is Math (aiismath) Conditional Probability section — the "Bayes' Theorem in Action" bag-of-basketballs SEE IT was removed (beta learners struggled with the example switching from coins to bags). It was replaced by a static "Update With New Evidence" ShowcaseBox that reuses the lesson's own 2-coin grid: new evidence ("the first coin landed on heads") greys out the tails-first outcomes and updates P(both heads) 25% → 50%. This was the lesson's only gated activity, so NextLessonGate moved from `visibleBayesSteps >= 4` to `ready: true`, and the orphaned `visibleBayesSteps` (useLocalStorage seeit-aiismath-bayesStep) + `BAYES_CONTINUE_LABELS` were deleted. The removed SEE IT is logged in docs/parking-lot.md. Lesson count unchanged (62). Note: the earlier standard-probability example in the same lesson was also reworked from a 2-coin question (P one-heads-one-tails) to a single-coin warm-up (P heads = 50%) followed by the 2-coin case (now P both-heads = 25%), with HEADS results highlighted in --green.

Update (2026-06-16): Why Learn AI? (whydeeper) was reworked. The "And it's early" prose became a NumberedRows box (title + intro), the steam-engine/electricity/internet parallels became the new Timeline component, the "Where AI sits in some jobs" ShowcaseBox was cut (parked in docs/parking-lot.md), and a TRY IT was added near the end: "You launch a hockey-jersey store" (STORE_TASKS, storeAnswers state), six jobs the student sorts "AI can shortcut it" vs "Still on you" (four power, two human-judgment curveballs at rows 3 and 6), grounding the earlier "shortened the distance between wanting and doing" line. The lesson was previously passive (NextLessonGate `ready: true`); Next now gates on `storeAllAnswered`.

Update (2026-06-16): What Is AI? (aihistory) gained the LoopLocator strip above all four concept kickers (Training/Patterns/Probability/Prediction). The top CoreLoopBox showed how the four ideas fit, but that structure was lost as the reader scrolled into the four parallel sections; the locator re-anchors "you are here in the loop" at each one. The four SectionKickers (TRAINING/PATTERNS/PROBABILITY/PREDICTION) were dropped since each locator's lit node already names the section, and each locator carries a doubled (48px) top margin so the concepts still read as separate blocks. The intro's "four ideas ... peanut butter" line moved to sit under the CoreLoopBox as its caption. No copy changed otherwise. (Locator redesigned the same day from a bordered strip to two tinted phase capsules with radio dots, matching the Claude Design reference.)

Update (2026-06-16): What Is AI? (aihistory) lost the white in-box "takeaway pill" from all four concept boxes (Training, Patterns, Probability, Prediction); each pill's text now sits as a plain body line below its box. The Training one came from the shared TrainingLoopBox, so its pill was removed at the component level and the takeaway re-added as body text in both lessons that use it (What Is AI? and Training). No copy changed beyond moving these lines out of the pills.

Update (2026-06-16): What Is AI? (aihistory) gained a closing TRY IT, "True or false?" (WHATQUIZ, quizAnswers state): seven True/False statements covering Training/Patterns/Probability/Prediction plus a few fun debunks (no humans typing in a data center, AI didn't grow up in Canada, read more than a person could in a thousand lifetimes), answers mixed T·F·F·T·T·F·F. The lesson was previously passive (NextLessonGate `ready: true`); Next now gates on `quizAllAnswered`.
- Finish Smarter (5): What You Learned (whatyoulearned), The Full Loop (fullworkflow), Vocab Quiz (keyterms), Test Yourself (testyourself), Beat the Clock (headtohead)

Update (2026-06-10, back-half restructure): the 5-part content spine became 6 parts. Check the Output slimmed to the checking skills (opener, howmuchtocheck, critical, verify, evaluating). A new Avoid Traps section (id openerprotect for its opener; reworked from Protect Yourself) unites the three trap families in three opener movements: traps in the answer (hallucination, trainingbias, documenttrap), traps in you (mindtrap, flattery, engagementtrap, supporttrap), traps from the world (faketrap). Seeing Isn't Proof was renamed The Fake Trap (id seeingisntproof -> faketrap; the old title survives as its kicker). Build Your Advantage split into The Future (openerrealworld reworked; workchanges, agents, aijudges, aifuture) and Build Your Skills (NEW opener id openerskills; integrity, privacy, humanedge, creativethinking, buildedge), ending the course on Build Your Edge. Who Else Is Affected (stakeholders) was removed: its four-roles ShowcaseBox moved verbatim into When AI Judges You after the THREE MODES box; its trade-offs box, Spot the Cost activity, and KeyInsights are parked in docs/parking-lot.md. StakeholdersSection, WhenNotSection, and the retired openers remain dead code. Gates rewired: evaluating -> openerprotect -> hallucination; documenttrap -> mindtrap; supporttrap -> faketrap -> openerrealworld; privacy -> humanedge; aijudges -> aifuture -> openerskills -> integrity. Course total stays 61. Print packet slugs follow the new group labels.

Structure notes (after the June 2026 back-half restructure): the spine is Start Smarter → Understand AI → Work With AI → Check the Output → Avoid Traps → Embrace the Future → Build Your Skills → Finish Smarter. Section badges in the lesson header read "Section · N of M" and derive from `SECTION_GROUPS`, not hand-typed; nothing in the file hardcodes the group count. Retired openers (openerusing, openercontrols, openertraps, openerjudgment, openerinside, openeranswers) plus the retired lessons (WhenNotSection, StakeholdersSection and their data) stay defined in the file as dead code, removed from the registries. The Avoid Traps opener keeps the component id `openerprotect` and The Future opener keeps `openerrealworld` (ids are stable across retitles, per house pattern); `openerskills` is the one net-new opener. Integrity/Privacy live in Build Your Skills as "the calls that stay yours."

Update (2026-06-08): Studying With AI was renamed Study with AI and moved from Protect Yourself into Start Smarter (placed after Why Bother Learning?), for its immediate value to students early in the course; Start Smarter went 7 → 8 and Protect Yourself 11 → 10. Choosing the Product and Choosing the Model were merged into a single lesson, Choose the App (surviving id `modelselection`); the former Choosing the Model component (`ChoosingModelSection`) is now rendered embedded as that lesson's second half (passed `embedded: true`, which suppresses its own header and Next button), and `choosemodel` was removed from `SECTION_GROUPS`/`SECTION_META`/`SECTION_COMPONENTS`. Work With AI went 10 → 9 and the course total 64 → 63. Course-wide, "product" was renamed to "app" wherever it refers to the AI app, so the three-decision framework is now App, Model, Mode; the hero illustration was renamed `illustrations/choosing-the-product.jpg` → `illustrations/choose-the-app.jpg` (same image kept) and still shows the old "Choosing the Product" slide title, so it (and its alt text) is pending a reshoot. Temperature was folded out of Work With AI into Inference (the last Understand AI lesson) as a closing beat: `TemperatureSection` now renders embedded (`embedded: true`) inside `InferenceSection` with prediction-anchored framing, and the standalone lesson's app/model/mode intro, prompting-tactics box, and Model/Mode KeyInsight were dropped; `temperature` was removed from `SECTION_GROUPS`/`SECTION_META`/`SECTION_COMPONENTS`, Thinking Mode now leads to Customization & Memory, and the Temperature glossary term plus two FAQ links repoint to `inference`. Work With AI went 9 → 8 and the course total 63 → 62; Understand AI stays 15 (Inference absorbed the content). The Thinking Mode lesson was renamed: id `thinkingmode` → `effort`, label "Thinking Mode" → "The Effort Dial", kicker "SPEED VS DEPTH" → "HOW HARD IT THINKS", and the three-decision framework is now App, Model, Effort (`ThinkingModeSection` keeps its function name; the id is the only registry change). Its body was reframed from the Direct/Thinking binary to an effort dial (cards "Quick answer" / "Deeper reasoning", quiz pills Quick/Deeper) to match how ChatGPT and Claude now expose reasoning effort; the Temperature glossary term plus two opener FAQ links repoint to `effort`, and stray "thinking mode" mentions in the Hallucination/Verify quizzes were reworded to "effort".

Update (2026-06-16): Study with AI (`studying`) was refocused on NotebookLM and its hands-on Lab ("Build Your Course Notebook"). The "Pick the Better Move" TRY IT (3 mint scenarios) was removed as redundant with the Best practices list, the tool-comparison box, and the Lab itself; its `picks`/`SCENARIOS`/`pickOption` are gone and the "Next: Foundations" gate is now `ready: true` (the Lab is take-home, so it can't gate). The cut scenarios are in `docs/parking-lot.md`. Section count unchanged.

Update (2026-06-17): Tokens (`tokens`) was reframed to motivate tokenization before naming it. The opener now shows a real chat exchange (static UserBubble/AIBubble on the "best Avengers movie" prompt) to make the humans-read-language vs. computers-read-numbers contrast concrete, then walks a failed naive fix (give every English word its own number → millions, and still can't cover unseen words) into the reusable-chunks idea, illustrated by a new "One chunk, thousands of words" ShowcaseBox (the `un` chip shared across unbelievable/unmatchable/untied). Only then are tokenization, tokens, and token ID named (the token-ID line now frames the ID as a meaningless address, bridging to Embeddings). The cat-vs-AI CompareBox and the "how text is split" examples are unchanged. The former "How the token vocabulary is built" section was cut down to a single line ("Once it’s built, the model uses that same fixed vocabulary..."); the per-model vocabulary sizes (ChatGPT ~200k, Gemini 256k, Claude not published) now sit inline in the tokenization paragraph instead. Copy/structure only; lesson map and counts unchanged.

Update (2026-06-17): Embeddings (`embeddings`) was reworked and the term **embedding vector** was deferred out of it. The intro after the Student-ID analogy now leads with the taste-test setup ("If someone asked you, 'Which drink has sweet of 9...'") and keeps only vector / dimension / value; the old "[9, 1, 10, 2, 3, 8] / order matters" line and the "embedding vector / starting meaning" paragraph were cut. The Taste Profile table gained a **Token ID** column (illustrative IDs >10k, muted gray) and a **Dimensions** group header over the six characteristic slots, with **Token / Token ID** column headings (top-level headers black/800, the line under Dimensions darkened); "Black Coffee" was renamed "Coffee" everywhere in Embeddings and Vector Space. Within Embeddings the lesson now says **vector** (cat→9246→vector box, the "Does every token get its own vector?" Q&A, and the Name That Drink activity). **embedding vector** is now first introduced in Black Box (`insidethemodel`), where the "five pieces" prose names it ("when one stands for a single token, pulled from the embedding table, it’s the token’s embedding vector") just before the MatchTerms checkpoint that tests it — so the term debuts where the embedding table and parameters do. Inference was unaffected (it already used "vector"/"starting meaning", not the term). Lesson map and counts unchanged.

Update (2026-06-17): the "Name That Drink" TRY IT in Embeddings was converted from sequential (Pattern 1: RevealSequence + mint QuizBlock, gated retry-until-correct) to parallel (Pattern 2: ScenarioRow + FeedbackPill, all three rows visible at once with per-option feedback; the trick "Not Enough Info" row keeps its per-pick feedback). The Pattern-1 `Takeaway` ("You read meaning straight from the numbers… that's exactly what a vector does for a token") was removed and re-homed as a `KeyInsight` that renders right after the activity once all three are answered (gated on `meaningComplete`). This activity was the source for `docs/components/try-it-takeaway.png` and the README's "TRY IT — completed, with Takeaway" example; since it no longer has a Takeaway, that screenshot was recaptured from "What Did the Model Actually Learn?" in Training Bias Trap (`trainingbias`, still sequential) and the README citation updated.

Update (2026-06-17, Understand AI "inside the model" reorder): the arc changed from `…tokens → embeddings → vectorspace → insidethemodel → attention → layers → training → prompt → inference` to `…tokens → embeddings → attention → layers → vectorspace → training → prompt → inference → insidethemodel`. Driving idea: Attention and Layers refine the embedded vector, so they follow Embeddings; Vector Space is the *payoff* of Layers' "Final Meaning" (a token's final point on the map), so it follows Layers; Black Box becomes the closing summary of the whole group. Black Box (`insidethemodel`) was un-overloaded by splitting "THE PIECES, INSIDE ONE BOX": (1) the `black-box-2.jpg` "Inside a real AI model" diagram + the embedding-table/embedding-vector/parameters naming moved to the **end of Embeddings** (an "INSIDE A REAL MODEL" capstone that replaced the simpler cat→9246→vector decimal box — so embedding table/vector/parameters now debut earliest, before everything that uses them); (2) the deep "70 billion parameters" paragraph was first moved to **Training**, then deleted entirely (2026-06-17) as redundant — parameters are now introduced in the Embeddings "INSIDE A REAL MODEL" capstone, and Training only touches them via "weights, also called its parameters" in the loop card (Training's old "you met in Black Box" ref was reworded to "also called its parameters" during the reorder, since Black Box is now after it); (3) the "Name the Piece" MatchTerms checkpoint + the learned-numbers KeyInsight stay in Black Box, which is now a pure end-of-group recap. Content surgery on the Layers↔Vector Space seam (final state 2026-06-17): split cleanly by job, with the bank example as the bridge. **Layers** is now purely the *process* and no longer has a "Final Meaning" section at all — after "Why so many layers?" it ends on the "representation, not an answer" KeyInsight. Its only illustration is `layers-1.jpg` (the inside-one-layer process diagram); the `layers-2.jpg` bank-journey board-game map was *moved to Vector Space* because it's really a vector-space picture (a word traveling past meaning-neighborhoods — money/vending/blood/river bank — and landing on its final position). **Vector Space** owns the product and now *opens on the bank* ("You just watched 'bank' travel through the layers and settle into one final vector… it's a **position**, a single point in a vast space of meaning"), then "where a point sits is what it means / similar meanings land close," then the `layers-2.jpg` map as the opening visual ("Here's that idea in one picture…"). Note: Vector Space now carries two map illustrations — `layers-2.jpg` (one word's disambiguation, opener) and `vector-space.jpg` (cross-word clustering: sodas/coffees/animals/vehicles, later); they show different facets (disambiguation vs clustering). The Coke/Pepsi/Coffee table is kept and reframed as the *measurement* demo (bridge: "the bank's final vector is real numbers, just ones we can't read; to make 'close' concrete, go back to numbers you can" — the taste test): it shows distance = sum of gaps and the "six dimensions can't separate Coke from Pepsi; add Citrus and they split" point, i.e. *why* real models need thousands of dimensions. Closes on prediction ("prediction itself is a map move" — Inference's SEE IT references "the meaning map from Vector Space"). Its taste-profile table was also reformatted to match the Embeddings table (Token / Token ID columns, Dimensions group header; Coke=24317, Coffee=51820 kept consistent with Embeddings, Pepsi=38106). Principle: Layers = how the numbers change; Vector Space = the final vector is a position in a space, and what that buys (similarity, distance, prediction). Eliminating Vector Space (folding into Layers) was considered and rejected because the map concept is load-bearing for Inference.

Update (2026-06-17, supersedes the split above — Vector Space merged into Layers): after repeatedly fighting the seam, Vector Space was merged in as Layers' closing section, dropping the standalone lesson. Mechanism: `VectorSpaceSection` now takes an `embedded` prop (suppresses its own `LessonHeader`/`LessonRule`/`NextLessonGate`) and is rendered at the end of `LayersSection` via `E(VectorSpaceSection, { embedded: true })`, followed by a new final KeyInsight ("By the last layer, meaning is a position… the next word it picks is whatever sits closest") and `Next: Training`. Trims taken in the merge: cut the `vector-space.jpg` neighborhoods illustration (the `layers-2.jpg` bank map is the one map now), the "Think of vector space like a city map" analogy, and the "close ≠ identical" aside; the prediction paragraph was dropped entirely (its point now lives in the final KeyInsight). The embedded section was then reorganized concrete-first (clearer than the old abstract-thesis opener): VECTOR SPACE kicker → "Let's go back to your taste test" → the Coke/Pepsi/Coffee table → "Coke and Pepsi sit closer than Coffee" → **distance** (the measure) → "Now back to bank: running the layers changed its vector into a brand-new row of numbers" → the catch ("that new set doesn't match any single word's vector; it's one of a kind") → "the idea of **vector space**: a map where similar words share a neighborhood" → the `layers-2.jpg` illustration as the payoff. `vectorspace` was removed from `SECTION_GROUPS`/`SECTION_META`/`SECTION_COMPONENTS`/`CHIP_LABELS` (the `VectorSpaceSection` function stays, used only embedded). Downstream: Inference's SEE IT now says "the meaning map from **Layers**" (was Vector Space); the Build-Your-Skills recap dropped "vector space" from "tokens, embeddings, layers"; a Test Yourself question's `lessonId` repointed `vectorspace → layers`. Understand AI 14 → 13; course total drops by 1. Layers' SECTION_META label was deliberately kept as "Layers" (the lesson still leads with layers and the onion icon fits; kicker "BUILDING UP MEANING" is vestigial/unrendered). NextLessonGate targets rewired accordingly (embeddings→attention, layers→vectorspace, vectorspace→training, inference→insidethemodel, insidethemodel→openerworkwith). Lesson map order changed; counts unchanged (Understand AI still 14, course total unchanged). This re-homes "embedding vector" into Embeddings' capstone, consistent with the earlier deferral (the lesson *body* still teaches plain "vector"; only the closing capstone names it).

Update (2026-06-18): the Inference lesson's opening flow box (InferenceFlowDiagram) and the "The Complete Journey" step-through were both replaced by a single static InferenceJourneyDiagram. Rebuilt from a Claude Design handoff (design_handoff_prompt_to_answer_flow/) into two phase lanes matching the purple/amber loop-box convention: Phase 1 "Read the message" (purple) holds Context Window, Tokens, Embeddings, Transformer (the emphasized "The core" card: attention then transformation across ~100 layers); a milestone bar bridges to Phase 2 "Write the reply" (amber) with Probability, Prediction, Output, Loop. Each lane uses a numbered spine (1-8) rather than repeated arrows. Variant A return-loop: an L-shaped rail in a reserved right gutter (RAIL_W 96px) runs the full height back to the top ("no memory · the whole journey runs again"); on narrow widths (<640px) the rail is dropped for a caption under the last step. The diagram carries its own lead paragraph (the prior BodyP lead-in was removed). Colors/spacing/type are the handoff's exact values; phase palette is raw hex (course ink tokens reused for text). The diagram copy is deliberately em-dash-free (the handoff had 3; kept out per course style).

Update (2026-06-18): the "Same question, different prediction" SEE IT (the Luke/Nate car demo), its following illustration (inference-2.jpg), and the now-redundant "One answer. Many picks." KeyInsight were moved out of Inference into Context Window (prompt), placed right before that lesson's "Same question, different answer. The context window is the difference." KeyInsight. Rationale: the demo's whole point is that the context window changes the output (it fits Context Window, which already tells the Luke/Nate story in prose), and Inference was carrying too many moving pieces while the demo reinforced only a couple. The demo's state (seeStep, the promptChars/aiResp* typing animations) now lives in PromptSection. Gating moved with it: Context Window's NextLessonGate now requires completing the demo (seeStep >= 6), and Inference's gate now keys on the "ChatGPT, decoded" reveal being fully opened (Object.keys(revealed).length >= COLS.length).

## Design system

### Tokens (CSS custom properties, defined in :root)
- Colors (surfaces): --bg #f6f5fb, --card #fff, --primary #6e51ff, --primaryDeep #4c2dff, --primaryFaint #f7f4ff
- Colors (text): --ink #0e0a1f, --inkSoft #3a3550, --inkMuted #6e6986, --inkFaint #b3aec8
- Colors (lines): --rule #e7e3f2
- Colors (semantic): --green #1f9d5f, --red #d4334a, --info #3b82f6, --info-bg #eff6ff
- SEE IT band: --seeBand #faf6ec (sand surface), --seeAccent #a36a17 (amber accent), --seeRule rgba(163, 106, 23, 0.35) (amber separator, commonly used as a 2px rule)
- KEY TERM band: --termBand #e9ecf0 (slate surface), --termAccent #475569 (slate accent)
- TRY IT band: --tryBand #eef4eb (mint activity surface), --tryRule rgba(63, 107, 63, 0.18) (mint hairline divider), --tryAccent #2f7d4f (green accent; mint InteractiveBox eyebrows and TRY IT Takeaway completions). The AIBubble reuses the same mint as its fill, documented under UserBubble/AIBubble.
- ILLUSTRATION band: --illoBand #f5e1d4 (warm sand surface), --illoAccent #b5532f (terracotta accent). Opener-support display band; see Illustration component.
- Typography: --sans (Plus Jakarta Sans, Google Fonts) for all reading text, --serif (Instrument Serif, Google Fonts) for display moments only (lesson titles, activity numerals, and opener/Illustration display phrases), never explanatory prose or feedback. --mono (system monospace stack). Source Serif 4 is no longer used; it was retired when all explanatory and feedback prose moved to sans.
- Shadows: --shadowSoft (0 4px 12px rgba(14, 10, 31, 0.05)) for inner cards, pills, and counters; --shadowElevated (0 8px 22px rgba(14, 10, 31, 0.05)) for support cards. Active-glow and a few one-off elevated/inset shadows remain inlined per-use, mostly on assessment and diagram surfaces.
- Body paragraph standard: 17px / 1.65 line-height / var(--inkSoft)

### Design philosophy
- Borders are rare in page-level chrome and narrative containers. ShowcaseBox, KeyInsight, KeyTerm, CompareCard, and InteractiveBox (mint and sand surfaces) all use fill + shadow with no border.
- Activity interiors may use --rule borders where the border carries meaning, separating choices, feedback, and interactive states.
- Two shadow tokens: --shadowSoft (inner cards, pills, counters) and --shadowElevated (support cards). Active-glow and one-off elevated/inset shadows are inlined per-use on assessment and diagram surfaces.
- Active states are singular: one section pill, one chip, one primary button per page.
- Serif (Instrument Serif) is for display moments only: lesson titles, activity numerals (SEE IT and TRY IT), and editorial display phrases inside opener Illustrations and late synthesis headings. Every other piece of text, including all explanatory prose and activity feedback, is sans (Plus Jakarta Sans). The old reading serif (Source Serif 4) has been retired.
- Page background never goes white.
- Vertical rhythm: card-level blocks (InteractiveBox, ShowcaseBox, KeyInsight) carry a default bottom margin of var(--blockGap) (24px); body paragraphs use 18px. Blocks render in normal flow, so adjacent margins collapse to the larger value and never stack. Pass marginBottom/marginTop to override. Spacing inside components and diagrams is still inline per-context; there is no global numeric scale beyond --blockGap.

### Components (defined near top of file)
- **BodyP**: standard body paragraph (17px / 1.65 / var(--inkSoft)). Props: marginBottom (default 18), style (additional overrides like maxWidth). Used for all lesson prose. Inline `<p>` is reserved for non-standard sizes (captions, card body text).
- **ActivityInstruction**: the one-line "how to do this activity" instruction that sits below the title inside an InteractiveBox (TRY IT or SEE IT), as the box's first child. Renders a `<p>` at 14px / var(--inkSoft) / line-height 1.55 / marginBottom 24. Every activity carries one where there's a real interaction cue; the text lives inside the box, not in prose above it. Use this instead of an inline `<p>` or the InteractiveBox `hint` prop for instruction text. Exceptions left without one: a few activities whose lead-in is genuinely just framing prose above the box (e.g., Welcome's opener TRY IT).
- **LessonHeader**: section-name eyebrow + serif title + optional subtitle. Eyebrow is a short rule line plus the section group name, uppercase, in var(--inkMuted) — no "SECTION NN" / "LESSON NN" numbers (position is already shown by the nav and sticky bar).
- **SectionKicker**: page-level (size="large", 18px) or pre-content (size="small", 13px). Both purple, uppercase, weight 700. Also serves as the in-body section break within a lesson — what used to be LessonSubhead is now a SectionKicker call. Default behavior is no kicker. **When to add one:** a SectionKicker signals a *new thought or topic turn*. Add it when the element it precedes introduces an idea the reader isn't already set up for; omit it when the copy directly above already establishes the subject (a kicker there just repeats what's been said). Quick test before any block (paragraph, ShowcaseBox, etc.): does the sentence right above already tell the reader what this is about? If yes → no kicker; if it turns to something new → add one. Examples on Why Learn AI?: the White House PullQuote has *no* kicker — the line right above it ("Here's how it describes what AI makes possible:") already sets it up, so the quote only delivers on it; "AND IT CUTS BOTH WAYS" *has* one because it pivots to a new idea (the flip side: AI quietly working against you).
- **ShowcaseBox**: framed callout for introducing or illustrating a concept, typically containing supporting cards. --primaryFaint fill, no border, borderRadius 20, padding 24. Props: kicker, headline, intro, marginBottom (default 24px), children.
- **KeyInsight**: structural takeaway callout. --info-bg fill (light blue), no border, borderRadius 14, 🔑 icon, inline layout. Props: lead (optional bold inline phrase), marginTop, marginBottom (default 24px each). Used for lesson-level zoom-out takeaways.
- **KeyTerm**: vocab introduction card. Slate band (--termBand) with 📖 icon and bold slate term (--termAccent). Props: term (required). Looks up the matching entry from the TERMS array, so vocab definitions in lessons cannot drift from the vocab quiz. Currently used in the No One Wrote the Rules lesson (norules) for "Guardrail."
- **InteractiveBox**: activity container. Variant "try" (✎) or "see" (◉). Surface "mint" (flat var(--tryBand), no border, 16px radius) or "sand" (var(--seeBand), no border, 24px radius, 32-36px padding). Sand surface renders the eyebrow in var(--seeAccent) amber. Default surface uses --primaryFaint with dashed border. Optional title/hint/action/children.
- **InnerCard**: standard white inner card used inside activities. White fill, 14px radius, --shadowSoft, default padding 22px 26px. Pass pad="snug" for the SEE IT stage size (18px 22px). Accepts a style prop for other overrides (display flex, marginBottom, custom padding) and children. Use instead of hand-building white cards inside InteractiveBox or RevealSequence.
- **ActivityCounter**: the "X of N" progress pill for an activity's InteractiveBox action slot. White pill, 999 radius, --shadowSoft, bold count plus uppercase "of N". Props: count, total. Convention: used only where items are revealed progressively, so the count tells the reader how far through a hidden sequence they are — reveal-sequence activities pass the visible card position, count = Math.min(currentIdx + 1, total) gated by the start latch so it reads 0 before starting and never jumps when an answer is picked. Parallel TRY ITs that show every item at once do NOT use it (removed 2026-06-18); the only parallel exceptions are the Vocab Quiz match game (matched-pairs progress, with its own round indicator) and the Build a Study Prompt form (fields-filled progress). Use instead of hand-building counter pills.
- **PrimaryButton**: purple fill, white text, hover lift. Auto-adds trailing arrow. Used for the opener advance button. Lesson-to-lesson navigation uses the card-based NextLessonGate.
- **ActivityButton**: dark-ink fill, white text. Used inside activities for in-activity actions. Quieter than PrimaryButton. Sizes: default and "large". (RevealSequence currently hand-builds visually matching buttons rather than calling ActivityButton; a future cleanup may consolidate them.)
- **NextLessonGate**: bottom-of-lesson navigation gated on a `ready` boolean. Renders a card-based control: a PreviousCard plus a NextCardUnlocked when ready, or a NextCardLocked when not. NextCardLocked renders the `lockedMessage` string (default "Complete activities to continue"). All lessons fall back to the default; the `lockedMessage` prop is still available for one-off overrides but is no longer set per-lesson. Card navigation is the intended pattern for this course.
- **OpenerSection**: standardized opener-page wrapper. Slots: LessonHeader (with optional bigIdea subtitle), whyThisMatters paragraphs, optional featuredCard, "BIG QUESTIONS WE'LL ANSWER" SectionKicker, whatYoullLearnIntro, ShowcaseBox containing the lesson-question cards (either a flat grid via `openQuestions` or a clustered display via `groups` where each group has a kicker label, a bridge subhead, and its own card grid), whatYoullLearnClosing, optional KeyInsight (lead "The common mistake:"), framing KeyInsight (lead "Keep this question in mind:" rendered from each opener's `question` prop), PrimaryButton. Each section group's opener is a thin wrapper passing content props, so format changes propagate to every opener.
- **Illustration**: opener-support display band, used as the featuredCard in OpenerSection (8 openers). Tinted band (--illoBand fill, no border, 20px radius, 24px 28px padding) with an uppercase --illoAccent eyebrow (default "◆ ILLUSTRATION"), optional headline and intro, and a children slot. The children slot holds the large Instrument Serif display phrases (clamp ~28-40px); this is the documented exception to the serif rule.
- **QuizBlock**: recessed --bg fill, --rule border. Statement is sans 22px/600/--ink. Per-option correctness via opt.correct.
- **RevealSequence**: state-machine progressive-disclosure driver for SEE IT activities. Manages currentIdx, started, per-stage state. Drives Pattern A (one stage swaps in at a time) and the ladder-mode sub-variant (cumulative reveal). Accepts a surface prop matching InteractiveBox.
- **Takeaway**: standard activity-completion takeaway, replacing the old centered emoji cards. Two-column grid (1fr / 2fr, 36px gap): left is an uppercase 11px eyebrow (default "The takeaway") above a 24px/600 sans headline; right is the 16px sans explanation. Props: headline, body, eyebrow, accent, merged. accent defaults to var(--seeAccent) amber for SEE IT; TRY IT completions pass #2f7d4f green. Standalone mode renders a white card (14px radius, soft shadow, 24px 28px padding); merged mode renders just a dashed-accent top divider plus the grid, for dropping inside an existing content card where rows or a table sit above (Tokens, Inference, Choose the App).
- **CompareCard**: tinted comparison card chrome. No border, 16px radius, 20px padding, soft shadow (0 8px 22px rgba(14, 10, 31, 0.05)). Props: bg (required tint color), centered (optional), style (optional). Canonical inner-block treatment: any visible container inside a CompareCard is white with no border.
- **CompareBox**: the comparison-group frame, distinct from ShowcaseBox and dedicated to the "X vs Y" comparison family. var(--primaryFaint) fill, 22px radius, 32px 40px padding. Renders an optional title above the grid, then a 1fr/1fr grid holding two ComparePanels. The title is either a `headline` (20px, 800, var(--ink) sentence-case box title — used by DoesAIThinkSection and the Tokens cat-vs-AI box) or, if no headline, an optional `kicker` (13.5px, 700, uppercase, 0.18em tracking, var(--primary)); both sit 18px above the grid. Props: headline, kicker, marginBottom (default 32), gap (default 18), children. Reference implementation: DoesAIThinkSection.
- **CoreLoopBox**: the shared "four ideas" anchor (Learn once: 📚 Training → 🧩 Patterns | Answer · every word: 🎲 Probability → ✍️ Prediction), purple/amber two-panel layout. Single source of truth, rendered in both What Is AI? (aihistory) and How We Got Here (howwegothere) so the two can't drift. How We Got Here re-shows it as an anchor, then tags each timeline milestone with the matching idea-chip (📚/🧩/🎲/✍️) so the timeline reads as the four ideas being invented one at a time, and the closing KeyInsight lands the payoff that Patterns (the transformer, 2017) was the last of the four to arrive.
- **LoopLocator**: the compact "you are here" echo of CoreLoopBox: a "0X/4" step number (the active concept's index, 24px/800 in the phase color, with a 15px var(--inkFaint) "/4" derived from `active` via a training=1…prediction=4 map), then two fully-rounded pill capsules joined by a var(--inkFaint) arrow — a lavender #ece8fd "Learn once" capsule (Training, Patterns) and a cream #fbedd3 "Every word" capsule (Probability, Prediction), each opening with a bold uppercase phase label (var(--primary) / #d97706). Each node is a 13px radio dot + 16px label. Props: active ("training" | "patterns" | "probability" | "prediction"); the active node fills its dot in the phase color and bolds its label to match, the other three show a hollow phase-tinted ring (rgba purple/amber ~0.4) and a var(--inkMuted) label. Heads each of the four concept sections in What Is AI? (aihistory) with no text kicker — a doubled 48px top margin (marginTop) sets each concept apart — so the loop structure stays in view as the reader scrolls past the top CoreLoopBox; the Training instance sitting right under the full box teaches the strip-is-the-box-shrunk mapping.
- **TrainingLoopBox**: the shared "reads → guesses → corrects" training-loop box (1·READS books/web/chats/code/images/audio/video, 2·GUESSES "peanut butter and __" → cloud ✗, 3·CORRECTS → jelly ✓). The old in-box "Repeat billions of times: this is training" pill was removed 2026-06-16; the takeaway now rides as a body line below the box in each lesson (a standalone "Repeat that loop billions of times, and that's training." in What Is AI?; woven into the weights paragraph in Training). Single source of truth, rendered in What Is AI? (aihistory) and in the Training lesson (training), where it folds in what used to be the predict/adjust/repeat stages. Training's "How LLMs Train" section is now one box per phase, each with a 20px black "Phase N · Name" title (Pretraining = set up → gather data → TrainingLoopBox → a one-line weights callback to Black Box; Instruction tuning = SFT; Preference tuning = RLHF), instead of a single box holding a flat 7-stage list.
- **CompareRows**: the aligned point-for-point member of the comparison family, for "X vs Y" content where row N on the left is answered by row N on the right. Same var(--primaryFaint) band geometry as CompareBox (22px radius, 32px 40px padding, optional 20px/800 `headline`), holding a split-tint inner panel (24px radius, hard 50/50 linear-gradient between the two column bgs) with two column headers (CompareHead-style type; left column left-aligned, right column right-aligned) above one white rows card (16px radius, var(--shadowElevated)). Each row: left item right-aligned in the left column color, a 40×12 double-headed arrow SVG (#8b7fc0), right item left-aligned in the right column color; rows split by var(--rule) hairlines. Pairing is structural: row j renders columns[0].items[j] against columns[1].items[j]. Props: headline (optional), columns (array of exactly two: { label, sub, emoji, color, bg, items }), marginBottom (default 32). Reference implementation: "When you think vs. what AI does" in DoesAIThinkSection. Use CompareBox/ComparePanel when the two sides are free-form bodies; use CompareRows when the comparison is a list of aligned one-line contrasts.
- **PullQuote**: the sourced-quotation card, for verbatim quotes from real documents or people. A var(--primaryFaint) band at 20px radius (no border or shadow), led by a 60px var(--primary) left curly quote mark; the quote sets in var(--serif) at 30px/1.4 var(--ink) (a sanctioned editorial display moment, no surrounding quote marks). Below it an attribution row: a short 26x2px var(--primary) rule plus an uppercase, 0.12em-tracked 13px/800 var(--primary) `cite` label, then a "· "-prefixed 14px/600 var(--inkMuted) `source`. Props: quote, cite (optional, the uppercase short label), source (optional, the muted publisher/date); legacy `attribution` still renders in the source slot if passed; marginBottom (default var(--blockGap)). Verbatim quotes keep their source punctuation, including em-dashes (write them as the backslash-u2014 escape so design-check's raw-byte count is unaffected); our own copy still avoids em-dashes. Reference implementation: the White House AI Action Plan quote in WhyDeeperSection.
- **NumberedRows**: the numbered-list display box, for showing an ordered list of practices, steps, or tips. A var(--primaryFaint) band (20px radius, 24px padding, optional 20px/800 `headline`) holding one white card (16px radius, var(--shadowElevated), 8px 28px padding) of rows split by var(--rule) hairlines. Each row: a 26px filled circle number (var(--primary) fill, white 13px/800 numeral), then a title line (optional emoji `icon` + BOX_CARD_TITLE/800 ink title), a body (BOX_TEXT + 1, inkSoft, 1.6), an optional `prompt` rendered as a quoted var(--mono) callout on a var(--primaryFaint) tile, and an optional `after` (body-styled line below the prompt tile; accepts a React node, e.g. a bold "What you'll get:" lead — used by the Flattery Trap strategies). Props: items (array of { icon?, title, body?, prompt?, after? }), headline (optional), intro (optional ShowcaseBox-style inkSoft 15px/1.6 subtitle below the headline, inside the band), marginBottom (default var(--blockGap)). body is optional: title-only rows render as a numbered checklist of bold one-liners (the "before you share" questions in PrivacySection). Reference implementations: "Best practices" in StudyingSection; "How to Fight It" in FlatteryTrapSection; "The five moves" in DocumentChatSection; "Five moves you can practice" in CreativeThinkingSection; the three "And it's early" advantages (headline + intro) in WhyDeeperSection. Use it whenever a lesson needs a list of named things with explanations; numbering is automatic.
- **NumberedColumns**: the numbered-card grid, for an ordered sequence shown side by side. An auto-fit grid (`repeat(auto-fit, minmax(125px, 1fr))`, 12px gap) of white cards (var(--card), 14px radius, 16px padding, var(--shadowElevated), column flex with 10px gap), each carrying a filled number chip (var(--primary) bg, white 13px/800 numeral, 8px radius, 28px min-width × 24px tall, self-start), a label (BOX_CARD_TITLE, 800, var(--ink), 1.25), and a body (BOX_TEXT, inkSoft, 1.55). No band of its own — compose it inside a ShowcaseBox or other band. Props: items (array of { label, body, muted? }); numbering is automatic. An item with `muted: true` renders its number chip in var(--inkMuted) instead of var(--primary), for a step that is deliberately not yours (the title stays standard ink). Reference implementations: "Here's the path: six moves, in order." in WelcomeSection; "AI does step 1. You do the rest." in WhyBotherSection (first item muted); "How AI absorbs work" in WorkChangesSection. Use NumberedRows when items need full-width explanations or prompt callouts; use NumberedColumns when the point is the sequence itself and each item is a few words.
- **Timeline**: the horizontal progression band, for an ordered run of eras/tools that lands on a present-day payoff. A var(--primaryFaint) band (20px radius, 24px 26px padding, optional 20px/800 `headline` and an inkSoft 15px/1.6 `intro` caption), then a CSS-grid track of equal columns joined by an absolutely-positioned 2px connector that fades from var(--rule) to var(--primary); each node is a centered 44px icon circle (white with a var(--rule) border) above an era label (BOX_CARD_TITLE/800) and a `transformed` line (BOX_TEXT, inkSoft). The final item is highlighted: its circle fills var(--primary) with a soft glow and its label + line render in var(--primary)/700. Props: items (array of { icon, era, transformed }; last is auto-highlighted), headline (optional), intro (optional), marginBottom (default var(--blockGap)). Reference implementation: the steam-engine → electricity → internet → AI run ("This is the rule, not the exception.") in WhyDeeperSection. Use it when the rhetorical point is "this keeps happening and the next one is here," not for a generic list.
- **ComparePanel**: one tinted column inside a CompareBox. Wraps CompareCard (panel tint, overridden to 18px radius and 22px padding) around a CompareHead plus a white InnerCard (12px radius, 18px 22px 20px padding, flex 1, keeps var(--shadowSoft)) that holds the body. The white body card is constant across every comparison box; the body inside it is a free slot formatted per content: bullets, code, a demo, an accent-colored monospace highlight line above dotted rows (the embeddings boxes), two labeled rows split by a hairline var(--rule) divider inside the one white card (Luke/Nate), or a centered hero such as a large emoji or number with the head left-aligned (the drink and cat boxes). Props: bg (panel tint), accent (head color), emoji (optional; omit to suppress on bodies that already carry an emoji, like the cat or drink boxes), eyebrow (optional; omit for an emoji-plus-title head like Luke/Nate), title, style (optional CompareCard override), children. For a body that genuinely should not sit on a white card, compose CompareCard plus CompareHead directly instead.
- **CompareHead**: the standardized panel header used by ComparePanel: optional emoji (15px, rides the eyebrow line) plus eyebrow (11px, 700, uppercase, 0.18em, accent color) plus title (24px, 700, -0.018em, line-height 1.15, accent color). Props: emoji, eyebrow, title, accent.
- Comparison family palette: blue panel #d6e2f4 with #2f4cb8 head, purple panel #ddd0ef with #5a3aa6 head. Comparison body text is 15px / 1.45 / var(--ink) with a 6px round var(--ink) dot at 0.45 opacity. ComparePanel keeps the 18/22 panel geometry as a convention, leaving CompareCard's 16/20 default untouched for its other uses. One sanctioned palette exception: Attention (attention) in HowReads keeps its own coding (var(--primary) head on a #6e51ff22 panel, #f59e0b head on a #f59e0b22 panel), because the three-solutions explorer just below color-codes the same two concepts in those colors. It uses STEP 1 / STEP 2 eyebrows since it is a sequence, not an opposition.
- **ScenarioRow**: wraps Pattern 2 row chrome. Serif numeral, optional kicker, prompt text, indented pill row, feedback strip. Props: index (1-based), kicker, prompt, answered, correct, feedback, headline, eyebrow, children. headline and eyebrow override the feedback strip defaults (eyebrow defaults to "Correct" / "Not quite", headline to "Right." / "Not quite."); several sorts pass them per item.
- **FeedbackPill**: pill button for Pattern 2 sort activities. Driven by booleans `answered`, `isCorrect`, `isPicked` (plus `label`, `onClick`); a legacy precomputed `state` string is still accepted but no longer passed by any activity. Four looks: neutral white (unanswered or untouched), green fill for a correct pick, solid red for the current wrong pick, pale red for a remembered earlier wrong pick. Each pill latches its own memory via an internal everWrong flag: once it has been a wrong pick it stays marked pale red even after the student switches answers. Green only ever appears on a pill the student actually picked; the correct answer is never auto-revealed.
- **UserBubble / AIBubble / ThinkingBubble**: chat-style speech bubbles, the standard way to show a prompt and/or an AI response. Each carries a dot-plus-uppercase eyebrow in its fixed role color (UserBubble violet --primary, AIBubble green #2f7d4f) regardless of surface. UserBubble has a lavender #ECEAFB fill, AIBubble a green-tinted #eef4eb fill, both flat with no border or shadow and a tail that follows alignment. Props: text, optional label (overrides the eyebrow word, e.g. "You · Vague prompt"), optional align ("left"/"right"; a paired exchange puts You on the right and AI on the left, while a single standalone message goes left), and optional visibleChars / visibleWords for typing animations. ThinkingBubble (left-aligned, animated ellipsis).
- **ProgressBar**: 220px header right slot, "X of N lessons" where N = SECTIONS.length, gradient track --primary→#b08eff.
- **useReducedMotion**: hook reading prefers-reduced-motion via matchMedia, updates mid-session. Returns boolean. Gates JS-driven typewriter and reveal animations so they tick at 1ms when reduced motion is requested.
- **WatchOverview**: reusable collapsible "Prefer to watch?" / "Prefer to listen?" media box (shared component near the top of the file). Faint-purple (--primaryFaint) outer band wrapping a white card with a purple play-icon tile, a WATCH (or LISTEN) eyebrow plus duration, and the title; a chevron rotates on expand. Collapsed by default; expands to a native `<video controls preload="metadata">` (no autoplay) — or, with `audio: true`, a native `<audio controls>` player — plus a caption. Props: `src` (media path, required), `audio` (true → audio player and the LISTEN / "Prefer to listen?" labels; default video), `title` (text after the bold heading), `duration` (e.g. "2 min"), `caption` (defaults to the NotebookLM "made from this lesson's notes" line; pass "" to hide). Open/closed is local state, not persisted. Uses: How We Got Here (`howwegothere`), self-hosted at `videos/How_We_Got_Here.mp4`; Does AI Think? (`doesaithink`), the Chinese Room NotebookLM podcast at `videos/Chinese_room.m4a` (`audio: true`). Screenshot in `docs/components/watch-overview.png` (video variant).

### TERMS array
Source of truth for vocabulary. Defined near the top of the file as a flat array of `{ term, definition, source }` entries. The KeyTerm component looks up definitions here, and the Vocab Quiz lesson uses the same array. The array currently holds 18 entries, but the quiz slices only the first 16 across two rounds of 8, so the last two entries (Transformation, Unstructured Data) are defined for KeyTerm lookups but not quizzed. Adding a new term in two places (the array and the quiz round count, if growing past 16) keeps lesson copy and quiz definitions aligned.

### Activity Patterns
TRY IT and SEE IT activities share a visual language and follow one of two interaction patterns each. Bespoke activities (text entry, redaction, builders, sliders) sit outside both patterns and stay one-off.

#### Shared visual language (TRY IT, both patterns)
- Container: InteractiveBox with `surface: "mint"`. Flat var(--tryBand) fill, no border, borderRadius 16, padding 26px 28px.
- Eyebrow: "✎ TRY IT" in Plus Jakarta Sans 700, 11px, letter-spacing 0.14em, uppercase, mint accent #2f7d4f.
- Title: Plus Jakarta Sans 700, 22px, --ink.
- Instruction line: the ActivityInstruction component as the box's first child, directly below the title (14px / var(--inkSoft) / line-height 1.55 / marginBottom 24). Applies to both TRY IT and SEE IT.
- Question numerals: Instrument Serif 400, 44px, line-height 1, color #2f7d4f. Opacity 0.6 when locked, 1.0 when active or answered.
- Question prompts: Plus Jakarta Sans 400 (non-bold, so the bold answer pills carry the emphasis), 18px, line-height 1.4, color #0e0a1f, max-width 56ch. Applies to both ScenarioRow prompts and mint QuizBlock statements.
- Answer pills: Plus Jakarta Sans 600, 15px, padding 14px 18px, borderRadius 12, 1.5px border. Untouched: white fill, --rule border, --ink text; hover var(--tryBand) fill + var(--tryAccent) border. Pills key off whether they are the one currently being viewed (dim-in-place): exactly one pill is fully saturated at a time, the one whose feedback is showing. Correct pick viewing: #1f9d5f fill, white text, white ✓. Correct pick dimmed: #e7f6ee fill, #b6e3cd border, #1f9d5f text and ✓. Wrong pick viewing: #d4334a fill, white text, white ✕. Wrong pick dimmed: #fbe9ec fill, #f0b8c1 border, #d4334a text and ✕. transition all 150ms.
- Feedback (mint QuizBlock): a two-column block below a dashed divider (1px dashed rgba(63, 107, 63, 0.18), marginTop and paddingTop 18), mirroring the SEE IT Takeaway. Grid 1fr/2fr, gap 24. Left column is a colored dot plus an uppercase 11px eyebrow, then a 22px/700 headline, both in the accent color. Right column is the 16px/400 body in neutral ink #0e0a1f. Accent #1f9d5f for correct, #d4334a for wrong. All sans; the only serif in the activity is the numeral.
- Counter pill (top-right of activity): white fill, soft shadow, "N OF M" (bold count, uppercase letterspaced "of M").

#### TRY IT Pattern 1: Progressive Disclosure
Each scenario is its own moment with setup + question + per-question feedback. Use when sequential pacing builds the lesson.
- InteractiveBox variant "try", surface "mint", title and counter action.
- RevealSequence drives advancement: state for currentIdx, started, answer-per-scenario.
- Inside RevealSequence children: a single white card (#fff, borderRadius 14, boxShadow 0 4px 12px rgba(14, 10, 31, 0.05), padding 22px 26px) wrapping the scenario intro (THE EVENT eyebrow plus setup, plus any chat bubbles or essay text) and the QuizBlock together. The TRY IT eyebrow, title, and counter stay on the mint above the card.
- QuizBlock (mint) is retry-until-correct. Wrong picks lock as red and never advance, and the correct answer is never revealed. onAnswer fires only on the correct pick, so parent gating (canAdvance, counter) needs no per-activity change.
- After solving, every option stays clickable for exploration ("see why this one is wrong"). Clicking any option shows its feedback and makes it the single loud pill (dim-in-place); all other results dim to a pale tint.
- Two-phase wrong content: while unsolved, a wrong pick shows a withholding hint that must not name the answer (the `hint` field; default generic). After solving, clicking a wrong option shows its full `feedback`, since the answer is already out.
- Option data: { label, correct, feedback, headline?, eyebrow?, hint? }. feedback is the right-column body; headline defaults to "Right." or "Not the best fit."; eyebrow defaults to "Correct" or "Not quite"; hint is the during-attempt nudge.
- Completion element: the Takeaway component with accent #2f7d4f (green) so it matches the mint shell.

#### TRY IT Pattern 2: Parallel Sort/Match
All items visible at once, sorted independently. Best for sorting/categorization tasks. Per-item feedback reveals as each item is answered.
- InteractiveBox variant "try", surface "mint", title only — no counter action. Every item is on screen, so an "N of M answered" pill is redundant (removed 2026-06-18 across all parallel TRY ITs); progress lives in the per-row answered state and the NextLessonGate. The counter is reserved for sequential reveals (Pattern 1, SEE IT Pattern A) where items are hidden until reached.
- All items render in vertical sequence with hairline dividers between rows: 1px solid var(--tryRule), padding 24 above/below per row, no divider above first row.
- Each row uses ScenarioRow: numeral on the left, content stack on the right, vertically centered.
- Pills indent to align under the prompt (paddingLeft equal to numeral width + gap, typically 66px).
- Per-item feedback reveals immediately after answer. Pills never lock: the student can re-pick freely. A correct pick fills green, a wrong pick fills solid red and stays remembered as pale red even after the student switches, and the correct answer is never auto-revealed (a pill turns green only when picked).
- NextLessonGate ready when `Object.keys(answers).length >= items.length`.

#### SEE IT Pattern A: Progressive Reveal, Sand Chrome
Stages reveal one at a time. Mirrors TRY IT Pattern 1 structurally; differs in color, shell, and the absence of QuizBlock.
- Shell: InteractiveBox with variant "see" and surface "sand". Sand band wrapper (var(--seeBand), no border, 24px radius, 32-36px padding). Eyebrow "◉ SEE IT" in var(--seeAccent) amber.
- Counter pill: identical to TRY IT visually.
- Engine: RevealSequence with surface "sand". It currently hand-builds its start/next/finish buttons (dark-ink fill, visually matching ActivityButton); a future cleanup may move these to the shared component.
- Stage interior: ivory inner card (white, 14px radius, soft shadow at 0 4px 12px rgba(14,10,31,0.05), 18-22px padding). Header row: serif amber numeral (Instrument Serif 32px, zero-padded "01" through "0N"), icon + Plus Jakarta Sans bold label (17px), Plus Jakarta Sans description (15px, color #3a3550). Subsequent sections separated by var(--seeRule) hairlines, each introduced by an amber 11px eyebrow with emoji prefix.
- Completion takeaway: the Takeaway component (default amber accent). Standalone when the completion is only the takeaway; merged (a dashed-divider footer inside the content card) when rows or a table precede it (Tokens, Inference, Choose the App).
- NextLessonGate: gates on full completion (activeIdx >= items.length).
- Ladder-mode sub-variant: stages stay visible cumulatively in one ivory card with var(--seeRule) hairlines between them. Most recent reveal animates in; earlier stages render statically. Used when stages build on each other (How LLMs Train, Three Steps to an Answer).
- No-completion-card variant: set isComplete: false on RevealSequence and gate canAdvance on `... && currentIdx < items.length - 1` so the activity ends on the last stage's content (Context "Same Prompt, Different Context").
- Pedagogical color preservation: SEE IT migrations recolor chrome to amber. They do NOT recolor interior content where color carries lesson meaning (Messy In's blue/red/purple Computer/Failure/AI palette, Context's role colors, Prediction's blue/purple/green steps).
- Decorative color order: for non-semantic card arrays (maps where color only distinguishes cards and carries no meaning), use the canonical palette in order, taking the first N: blue #3b82f6, green #10b981, amber #f59e0b, purple #8b5cf6. Colors that carry lesson meaning are exempt and follow the rule above.

#### SEE IT Pattern B: Parallel Reveal, Sand Chrome
Comparison matters more than sequence. All stages render at once as ivory cards stacked inside the sand band, separated by var(--seeRule) hairlines. No RevealSequence, no counter pill.

## Lesson structure

### End-of-lesson transitions
A lesson ends on its landing beat (Key Insight, Takeaway, or completed activity). It does not end on a forward-pointing paragraph. The NextLessonGate button is the structural pivot; a "...next lesson, we'll..." sentence dilutes the insight and adds reading load.

If a transition is genuinely needed for orientation, it goes at the top of the next lesson, not at the bottom of the current one. Each lesson reads as a standalone unit; the next lesson sets up its own question.

Practical rules:
- The last component before LessonRule should be a Key Insight, Takeaway, or activity-completion beat. Not a BodyP that pivots forward.
- Key Insight bodies should not trail off with "...next we'll..." or "...that's the idea behind X." Trim those sentences.
- Forward references inside mid-lesson copy are fine. The rule applies to the lesson's final beat.

## Working agreements

### David's setup
Designs prompts in chat with Claude (this Project). Pastes prompts into Claude Code in his VS Code terminal, where they get applied to the file. Verifies changes in his browser. Repo: AI-Training, single index.html file, deployed via Vercel.

### Course PDF export
A print-mode view is available via the URL parameter `?print=all`, with a "Print Course PDF" link beneath Reset Progress in the LLMExplorer footer. Renders every lesson in sequence with a page break between lessons, bypasses the name gate, and uses no-op nav callbacks. Component-level overrides expand activities into static, fully-answered form so the printable output is self-contained: ActivityCounter renders null, RevealSequence renders all children plus its completionElement and skips the start/next/finish buttons, and mint-surface QuizBlock renders the title row plus each option in answered state with full feedback below. Not yet expanded for non-mint QuizBlock surface, custom interactive widgets, or multi-scenario activities (those show only the first scenario in print mode). Full multi-scenario expansion would require per-lesson refactoring. The same `?print=<group-slug>` route renders a single part for download (it used to render the live `?print=start-smarter` page that students saved via Cmd+P). Update (2026-06-16): Lab 01 ("Build Your Course Notebook", studying) no longer sends students through the browser print dialog; step 1 now links a pre-generated static PDF at `packets/start-smarter.pdf` (the `download` attribute names the saved file "Start Smarter.pdf"). That PDF is a snapshot of the whole Start Smarter part, so it goes stale when any of those 8 lessons change — regenerate it with `bash scripts/make-packet.sh` (headless Chrome `--print-to-pdf` against `?print=start-smarter`) and re-commit.

### Illustrations
Stored in `illustrations/` and referenced as `<img src="illustrations/<name>.jpg">`. House format is JPEG at 1200x800 (~250-350 KB). Display size is set per-image via the img's `maxWidth` (most are 560; the info-dense "What's an LLM?" is 880). Use URL-safe filenames: lowercase, hyphens, no spaces, apostrophes, or `?`. New illustrations usually arrive as large PNGs; optimize before use with macOS `sips`, then delete the source PNG and point `src` at the `.jpg`:

`sips -s format jpeg -s formatOptions 80 --resampleWidth 1200 illustrations/<source>.png --out illustrations/<name>.jpg`

### Preferences
- Plain copy, McDonald's-humor friendly tone for the student audience (16-year-olds).
- Implementation-ready prompts referencing exact line numbers, existing patterns, and variable names.
- No file downloads, always copyable code blocks.
- David avoids em-dashes, and new course content avoids them too. The 7 existing uses in the file are the design-check.sh baseline; new copy stays at zero.

### Default mode
Collaborative content design. Propose options, ask for confirmation, then draft prompts. When David has clearly decided something, skip straight to the prompt. When something is ambiguous, ask before drafting.

### The pattern for content changes
1. Read the current state of the relevant section in index.html before suggesting copy or structure.
2. Propose options or ask clarifying questions if ambiguous.
3. Once aligned, draft the prompt as a code block.
4. After David runs it, ask what he saw in the browser and iterate if needed.

### When David returns with twin-testing feedback
Name the lesson, describe what felt off, read the relevant section in index.html before proposing options. Twin feedback might be a copy fix, an interaction fix, or a sign the lesson itself needs rethinking. Figure out which before drafting any prompt.

- Pre-ship governance: run `bash design-check.sh` from the repo root before shipping a new lesson. It flags hand-built counter pills, raw fonts, literal shadows, non-token mint dividers/surfaces, and em-dashes against known baselines. A deliberate diagram or assessment exception is fine; bump that check's expected count so the baseline stays honest.

### Assessment and diagram exceptions
Some late-course lessons intentionally step outside the standard component rhythm. These are exceptions, not patterns. Do not copy their chrome into ordinary lessons.

Known exception zones:
- Beat the Clock (headtohead): a timed assessment with custom game states (intro, playing, done), its own timer, score display, and stateful buttons.
- Late Build Your Advantage and Finish lessons: bespoke diagrams, flowcharts, score cards, and assessment panels where the visual itself carries the instructional meaning.
- RevealSequence start/next/finish buttons: hand-built rather than ActivityButton. Known, slated for a later cleanup.
- Custom shadows: active glows and a few elevated/inset shadows live inline on these surfaces. var(--shadowSoft) and var(--shadowElevated) remain the default everywhere else.

Boundaries when building an exception, so it still reads as part of the course:
- Use the course color tokens. Do not introduce a new raw palette.
- Use the standard type: var(--sans) and var(--serif). Do not add a new font.
- Reuse InnerCard, ActivityButton, and ActivityCounter wherever they fit.
- Include a standard or compact LessonHeader where feasible so the lesson stays in the course frame.

An exception is never a reason to skip tokens or standard type. If you deliberately add a new custom shadow or other flagged pattern, bump its expected count in design-check.sh so the pre-ship check stays meaningful.
