# Embedding lookup illustration

Edited with the built-in image_gen tool on 2026-09-06.
Source: `inside-real-model-summary.png` in this directory.
Output: `embedding-lookup-only.png` in this directory.
Final board: `lessons/embeddings-inside-real-model-editorial.jpg`.
Renderer: `render_inside_real_model` in `scripts/video/render_understand_ai_retrofit_review.py`.

## Prompt

Use case: precise-object-edit. Edit target: the supplied wood-and-purple embedding-table illustration. Remove ONLY the stack of three black machines on the far right and the dotted glowing purple cable that curves from the highlighted bottom table row toward the machines. Fill their former area naturally with the same dark wood backdrop. Preserve EXACTLY the geometry and positions of the left cat tile, left blank ID display, large central wooden table frame, all eight columns and seven rows including the header, the blank table cells, and the purple outline around the bottom row. Preserve the complete glowing rectangular border of the bottom row, but remove the cable that extends outside the table. All table cells and the ID display must stay blank for programmatic text. Keep the same camera, aspect ratio, lighting, materials, textures, and framing. Do not move or resize any remaining objects. No text, numbers, arrows, captions, or new objects.
