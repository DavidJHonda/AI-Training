SYSTEM INSTRUCTION: You must strictly adhere to all visual, narration, pacing, and containment rules in your source document named "Master Prompt". Do NOT read those guidelines aloud. Scene instructions below take precedence over any general rule in that document.

CRITICAL CONSTRAINTS (STRICT):
1. NO TITLE CARD: Never render the lesson title (or any title) as an opening screen or pill. Open directly on Scene 1's first visual on the light grid-paper canvas; that first frame sets the style. The narrator does NOT say "welcome" and does NOT read the title aloud; narration begins directly with the hook.
2. NO SCREENSHOTS / NO PHOTOS: Never screenshot paragraphs, text blocks, tables, or UI boxes, and never show the lesson document itself on screen. Draw every visual fresh. No photographic or stock imagery of any kind — no Getty or watermarked images, no photos of people, statues, or places.
3. ONLY THE SCENES LISTED: Render exactly the scenes below — no extra b-roll, interstitial, chapter-title, or decorative scenes. All on-screen text comes verbatim from the scene list; never render annotation-style scribble text or invented statistics.
4. LIGHT CANVAS, DARK ACCENTS ONLY: Every scene sits on the light grid-paper canvas; "dark" refers only to individual pills or cards. A full-frame dark background is forbidden.
5. NUMBERS: all values on screen come from the attached boards; invent none.

FOCUS: Analyze the attached file "vector-space.md" verbatim.
TOPIC: Vector Space (Meaning Is a Position)

🎬 SCENE BREAKDOWN:
- SCENE 1: The Unmatchable Vector. (the video's FIRST visual — no title card before it). Sketch a token pill trailing a fresh row of numbers, an arrow pointing at a small sketched lookup table crossed out with a red X. Write NO text inside the drawing. One black-ink label: "Matches nothing on file."
- SCENE 2: The Taste Test. (HARD CANVAS WIPE). Reproduce the attached illustration "vector-space-taste.jpg" — the "Taste Profile · Coke vs. Pepsi vs. Coffee" table — every word and number crisp, held static while narration compares the rows. Do not substitute your own visuals; add NO text beyond what it carries; no element of it appears in any other scene.
- SCENE 3: Meaning Is a Position. (HARD CANVAS WIPE). Reproduce the attached illustration "vector-space-map.jpg" — the vector-space map with the word neighborhoods and the path of IT from ambiguous start to landing by "cat" — held static while narration walks the concept and the journey. Same reproduction rules; no element of it appears in any other scene.
- SCENE 4: Not Just Pronouns. (HARD CANVAS WIPE). Reproduce the attached illustration "vector-space-airplane.jpg" — the two airplane sentences with their "Nearby on the map" chips — every word crisp, held static. Same reproduction rules; no element of it appears in any other scene.
- SCENE 5: The Close. (HARD CANVAS WIPE). One dark glassmorphism pill: "Meaning is a position." One sticky note: "The final vector is the blend of the whole sentence."

🗣️ NARRATIVE ARC:
Hook: in the last lesson you watched a token ride the layers, its numbers changing at every step — and you left with a blank box: where do IT's numbers finally land? Out of the layers comes a fresh vector, a new row of numbers that captures what the token means. In theory the model could look that vector up in its table of token embeddings. It can't: the transformed numbers are one of a kind, matching no token in the table. So how does AI work out what the token means? Go back to the taste test from Embeddings: Coke, Pepsi, and Coffee, each described as a row of numbers across seven dimensions. Coke's and Pepsi's rows sit much closer to each other than either sits to Coffee — — exactly how the model measures meaning: distance, adding up the gap in every dimension. The model has established Coke is more like Pepsi than like Coffee. The same move answers the one-of-a-kind vector: the model doesn't look its numbers up — it reads the token's position. That's vector space: a map where every vector sits somewhere, and similar words share a neighborhood. Watch the sentence from the Transformer lesson: how does AI establish the meaning of IT? On its own, IT is just a pronoun — it could mean almost anything, and certainly not CAT. At each layer its numbers shift as the model reads the surrounding words. In vector space, the final numbers for IT land closer to CAT than to any other word in the sentence: the model has figured out that IT means the cat. And it's not just pronouns — every token gets rewritten by the ones around it, even a plain, concrete noun. Watch airplane in two sentences: folded paper and room pull it toward paper, fold, toy, glide; roared, runway, and sky pull it toward jet, runway, pilot, flight. Same word, same starting vector — but it isn't one keyword flipping a switch: the final vector is the blend of the whole sentence. Close: meaning is a position. Change the words around it, and the same token lands somewhere new.
