# how-the-model-learns-2: spans of >= 6s with no scene cut, and the
# narration underneath. Free under the dead-time rule while the
# narration is still walking what is on screen; charged once it
# has moved past it. Read both columns before deducting.

== HOLD 0:09.83 -> 0:15.83  (6.0s)
   narration: If you've used ChatGPT, Claude, or Gemini, you were interacting with an application. ChatGPT is the app. The LLM is the engine.

== HOLD 0:19.83 -> 0:30.67  (10.8s)
   narration: Under the hood, a large language model, or LLM, does the work. These models are called large because they are trained on huge amounts of text and code. Language describes what they do. They work with words to read, write, summarize, translate, and explain information.

== HOLD 0:30.70 -> 0:43.13  (12.4s)
   narration: Language describes what they do. They work with words to read, write, summarize, translate, and explain information. And the model uses learned numerical patterns to predict likely output based on that data. These numerical patterns arise from a systematic cycle of trial and error called training.

== HOLD 0:53.77 -> 1:23.27  (29.5s)
   narration: This diagram shows the four-step loop that builds the model's knowledge. Step 1 is read. The process takes a training example with a known answer, like peanut butter and. Step 2 is guess. The model attempts to predict the next word. In this case, it might guess Claude, which is incorrect. Step 3 is check. The system compares its guess against the actual word in the example, which is jelly. Step 4 is adjust. To fix the error, the system alters the model's internal numerical weights

== HOLD 1:23.27 -> 1:31.30  (8.0s)
   narration: Step 4 is adjust. To fix the error, the system alters the model's internal numerical weights so that jelly becomes a statistically more probable guess next time.

== HOLD 1:31.30 -> 1:44.33  (13.0s)
   narration: so that jelly becomes a statistically more probable guess next time. The AI is not memorizing a rulebook. It is adjusting statistical probabilities through these internal numbers. A single correction doesn't finish the job. One adjustment only makes a slight change to the model's behavior.

== HOLD 1:44.33 -> 1:51.20  (6.9s)
   narration: A single correction doesn't finish the job. One adjustment only makes a slight change to the model's behavior. The model then moves to the next example and repeats the loop. This cycle happens across billions of different sentences.

== HOLD 1:51.20 -> 2:02.17  (11.0s)
   narration: This cycle happens across billions of different sentences. Across those billions of repetitions, millions of tiny numerical adjustments accumulate. This brute force repetition of mathematical adjustments is what transforms random guesses into accurate predictions. What actually results from all those mathematical adjustments?

== HOLD 2:06.17 -> 2:25.70  (19.5s)
   narration: What actually results from all those mathematical adjustments? The result is learned patterns, beginning with simple word associations, like the link between peanut butter and jelly. You recognize these patterns too. After twinkle, twinkle, little, you know the next word is star. Once a panna leads to time, and better late then leads to never. Simple phrases are easy to spot, but the model identifies patterns at a much larger scale.

== HOLD 2:31.53 -> 2:51.33  (19.8s)
   narration: That same mechanism identifies recurring structures and how we communicate. By processing billions of examples, it maps patterns used to build arguments, write code, or explain ideas. These structures result directly from shifting numbers. With every adjustment, the internal map of language becomes more precise. The patterns emerge organically as the math stabilizes.

== HOLD 2:54.77 -> 3:01.43  (6.7s)
   narration: Training has built patterns into the model's internal numbers. Now let's see how the model uses those patterns to build an answer.
