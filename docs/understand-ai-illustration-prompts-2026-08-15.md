# Understand AI illustration prompts — 2026-08-15

Generated with the built-in image generation tool. The two source images below were supplied as identity and visual-style references only, not edit targets:

- `illustrations/welcome.jpg`
- `illustrations/does-school-matter.jpg`

All selected images use a 3:2 landscape composition and were exported as 1200×800 JPEGs. The shared direction was: preserve the recurring hosts (Luke, curly light-brown hair, green hockey jersey 4; Nate, dark curly hair, green hockey jersey 96); warm cinematic editorial photorealism; tactile brass-and-glass educational machinery; forest green, cream, and amber; believable faces and hands; exactly two people unless a puppy is specified; no technology-company logos, watermarks, brains, humanoid robots, or dense infographic text.

## Opener — `opener-understand-v2.jpg`

Luke and Nate stand at an open hood. Instead of an ordinary engine, the compartment contains an elegant tangible AI mechanism: layered brass-and-glass components, token tiles, glowing connections, and a compact mathematical core. They are curious and mechanically engaged, looking underneath rather than treating it as magic. Strong single composition with ample breathing room. Include only the perfectly legible headline `UNDER THE HOOD`. No branded technology, CEO hats, or fantasy magic effects.

## Training — `training-v2.jpg`

A single model core moves through three clearly distinct workshop stations from left to right while Luke and Nate observe and operate the process. Station one feeds books and text cards into the core to learn patterns. Station two uses question-and-ideal-answer conversation cards to teach direct responses. Station three uses ranked answer cards and human preference markers. At the far right, the finished model core is sealed and ready to use. Use only the labels `PRETRAINING`, `INSTRUCTION`, and `PREFERENCE`. Make it feel like training one machine through stages, not creating a human mind, and do not imply training continues during use.

## AI Is Math recap — `ai-is-math-2-v2.jpg`

Show exactly two people in one continuous scene at a single long workbench. On the left, Luke handles a jar of light and dark probability balls representing a base rate. In the center, Nate adds a new clue by pointing to a humidity gauge while rain clouds gather outside; a connected row of weighted balls shifts toward rain. On the right, a brass mechanical word track builds a sentence one physical tile at a time and chooses the final tile `RAIN`. Include only `BASE RATE`, `NEW CLUE`, `NEXT WORD`, and the tiles `IT IS GOING TO RAIN`. No percentages, equations, triptych, repeated people, or inaccurate statistics.

## Tokens — `tokens-v2.jpg`

Luke feeds a strip of ordinary written text into a compact mechanical tokenizer while Nate examines the output. Separate colored tiles cleanly split one word into reusable chunks: `UN | BELIEV | ABLE`. Beneath each chunk is a small numerical ID tag, showing that text is converted to numbered pieces before the model sees it. Make the cutting and number assignment obvious. Use only `TEXT`, `TOKENS`, `TOKEN IDs`, the chunk labels, and simple four-digit ID numbers. Do not imply that a token ID contains meaning.

## Embeddings opening — `embeddings-1-v2.jpg`

Luke and Nate conduct an unbranded blind taste test with exactly three drinks: two similar cold dark fizzy colas labeled A and B, and a visibly different hot coffee labeled C. In front of each drink is a horizontal row of colored slider beads representing sweet, bitter, fizz, heat, and caffeine. The patterns for A and B should be close while C is clearly farther away. A brass measuring grid transforms the qualities into rows of numbers, communicating that meaning can be represented as a position made of many numerical dimensions. Include only the headline `MEANING AS NUMBERS`, drink labels A/B/C, and simple numeric rows. No beverage branding or cloned people.

## Transformer — `transformer-v2.jpg`

Luke and Nate stand over a circular brass-and-glass table displaying every word in the sentence simultaneously as physical tiles: `THE CAT SAT ON THE MAT BECAUSE IT WAS TIRED`. The tile `IT` glows and sends a strong bright attention connection to `CAT` and `TIRED`, while faint lines reach less relevant words such as `MAT`. The composition must make contextual meaning and attention obvious and must not imply left-to-right reading one word at a time. Include only `ALL AT ONCE`, `ATTENTION`, and the sentence tiles.

## Layers — `layers-v2.jpg`

Luke and Nate inspect a tall stack of transparent glass processing layers. A physical token tile labeled `IT` enters the bottom diffuse and ambiguous. As it rises through repeated layers, glowing attention connections from the surrounding word tiles `CAT`, `MAT`, and `TIRED` repeatedly refine its pattern. At the top it exits clear and bright with a strong connection to `CAT`. Make repeated passes and progressive refinement visually obvious. Use only `LAYER 1`, `LAYER 2`, `LAYER 3`, `DOZENS MORE`, and `IT → CAT`.

## How AI Answers — `how-ai-answers-v2.jpg`

Luke and Nate sit with a friendly new puppy while using a brass-and-glass word-prediction machine to answer what to name the dog. A ranked selector contains several name tiles at different weighted heights; `SPOT` is selected. A conveyor builds the answer one separate token tile at a time: `YOU | COULD | NAME | HIM | SPOT`. Include only `ONE TOKEN AT A TIME`, the answer tiles, and ranked tiles `SPOT`, `MAX`, `BUDDY`. Exactly two people plus one puppy; no chatbot screen.

## One More Thing — `one-more-thing-v2.jpg`

Luke and Nate stand at the center of one connected machine with three mechanisms. Left: a weighted lottery drum full of unequal numbers of candidate-word tickets, showing controlled randomness. Center: a long conversation-transcript reel loops back through the machine before each next word, showing that the model rereads the available chat rather than remembering like a person. Right: a vast receding array of brass calculation wheels and server-like arithmetic modules, showing staggering mathematical scale. Connect the three mechanisms as one inference process. Use only `RANDOMNESS`, `NO MEMORY`, `MATH AT SCALE`, and `NOT A MIND. MATH AT SCALE.` No exact statistics or equations.
