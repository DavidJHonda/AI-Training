# Briefing — Structural Changelog (archive)

Historical record of lesson-map and structural changes, moved out of briefing.md on 2026-07-01 when the briefing was slimmed to a current-state + house-rules doc. **This is an archive — git history is the live source of truth.** Many entries here supersede earlier ones (e.g. Vector Space was merged into Layers, then later re-split back out). Do not treat any single entry as current; check index.html / SECTION_GROUPS for the live structure.

---

Update (2026-06-10): When Not to Use AI (whennot) was removed and merged into How Much to Check (howmuchtocheck), which simultaneously moved from Protect Yourself into Check the Output as that section's first lesson after the opener ("set the dial" before the checking skills). The merged lesson gained a "SOMETIMES THE ANSWER IS NO" beat (the four don't-reach-for-it categories, moved verbatim) and a seventh Make the Call scenario ("Send it for me?" / Acting for you); whennot's KeyInsight (the say-it-out-loud test) was folded into Academic Integrity's KeyInsight body; its two scenarios that duplicated Make the Call rows and the "A friend's secret" privacy scenario were cut (logged in docs/parking-lot.md). Gates rewired: openercheck → howmuchtocheck → critical, supporttrap → integrity. The Check the Output opener gained a third overview group (SET THE DIAL, first). WhenNotSection and WHENNOT_SCENARIOS remain in the file as dead code. Check the Output 7 → 8, Protect Yourself 10 → 8, course total 62 → 61.

Update (2026-06-15): Mess to Meaning (data) was merged into Rules vs Patterns (aivscode), since the rules-vs-patterns contrast and the mess payoff are one idea. The merged lesson keeps both CompareBoxes, the PS5 SEE IT, and both TRY ITs (Which One Handles This?, then What Could AI Pull Out?), with the Next gate now requiring both activities (fitAllAnswered && fuelAllAnswered); data's "AI runs on patterns" opener was replaced by a bridge paragraph back to the first TRY IT, and its "The move, step by step" ShowcaseBox was cut (parked in docs/parking-lot.md). Harder to Control (norules) stays standalone and now gates to AI is Math (aiismath). DataSection stays in the file as a dead stub (returns null), removed from SECTION_GROUPS/SECTION_META/SECTION_COMPONENTS; the Unstructured Data glossary term repoints from data to aivscode. Understand AI 15 → 14, Foundations 6 → 5, course total 63 → 62. (While updating these counts, two pre-existing lesson-map drifts were reconciled against SECTION_GROUPS: Choose the Model (choosemodel) had been re-split into its own standalone lesson in Work With AI after the 2026-06-08 merge note, and Be Curious (becurious) had been added to Build Your Skills; neither was logged, so the map and total had read 2 low. The live app counts 62.) Update (2026-06-16): Rules vs Patterns (aivscode) was re-pitched as one idea rather than two — a single cause→effect arc (AI runs on patterns, not rules, which is *why* it makes sense of messy/unfamiliar input). Its two CompareBoxes were merged into one taller box (each column now flows what-it-needs → so-how-it-acts → superpower, folding the structured-table-vs-modalities visual into the "what it needs" row), and its two TRY ITs collapsed into one fused "Which One Handles This?" activity: the rule-vs-pattern discrimination stays the learner's choice, while the pattern-win feedback now reveals what AI pulls out of the mess (voice memo → to-dos; grocery photo → checklist plus the "can't invent" guardrail). The scam-text row was swapped for the grocery-photo row, and the standalone "What Could AI Pull Out?" TRY IT plus two bridge paragraphs were cut (parked in docs/parking-lot.md). SEE IT now precedes the merged box (experience before systematize); the Next gate drops to a single flag (fitAllAnswered), and the "It restructures; it doesn't invent" KeyInsight survives, now triggered by the one activity. Lesson count and course total unchanged (62).

Update (2026-06-15): AI is Math (aiismath) Conditional Probability section — the "Bayes' Theorem in Action" bag-of-basketballs SEE IT was removed (beta learners struggled with the example switching from coins to bags). It was replaced by a static "Update With New Evidence" ShowcaseBox that reuses the lesson's own 2-coin grid: new evidence ("the first coin landed on heads") greys out the tails-first outcomes and updates P(both heads) 25% → 50%. This was the lesson's only gated activity, so NextLessonGate moved from `visibleBayesSteps >= 4` to `ready: true`, and the orphaned `visibleBayesSteps` (useLocalStorage seeit-aiismath-bayesStep) + `BAYES_CONTINUE_LABELS` were deleted. The removed SEE IT is logged in docs/parking-lot.md. Lesson count unchanged (62). Note: the earlier standard-probability example in the same lesson was also reworked from a 2-coin question (P one-heads-one-tails) to a single-coin warm-up (P heads = 50%) followed by the 2-coin case (now P both-heads = 25%), with HEADS results highlighted in --green.

Update (2026-06-16): Why Learn AI? (whydeeper) was reworked. The "And it's early" prose became a NumberedRows box (title + intro), the steam-engine/electricity/internet parallels became the new Timeline component, the "Where AI sits in some jobs" ShowcaseBox was cut (parked in docs/parking-lot.md), and a TRY IT was added near the end: "You launch a hockey-jersey store" (STORE_TASKS, storeAnswers state), six jobs the student sorts "AI can shortcut it" vs "Still on you" (four power, two human-judgment curveballs at rows 3 and 6), grounding the earlier "shortened the distance between wanting and doing" line. The lesson was previously passive (NextLessonGate `ready: true`); Next now gates on `storeAllAnswered`.

Update (2026-06-16): What Is AI? (aihistory) gained the LoopLocator strip above all four concept kickers (Training/Patterns/Probability/Prediction). The top CoreLoopBox showed how the four ideas fit, but that structure was lost as the reader scrolled into the four parallel sections; the locator re-anchors "you are here in the loop" at each one. The four SectionKickers (TRAINING/PATTERNS/PROBABILITY/PREDICTION) were dropped since each locator's lit node already names the section, and each locator carries a doubled (48px) top margin so the concepts still read as separate blocks. The intro's "four ideas ... peanut butter" line moved to sit under the CoreLoopBox as its caption. No copy changed otherwise. (Locator redesigned the same day from a bordered strip to two tinted phase capsules with radio dots, matching the Claude Design reference.)

Update (2026-06-16): What Is AI? (aihistory) lost the white in-box "takeaway pill" from all four concept boxes (Training, Patterns, Probability, Prediction); each pill's text now sits as a plain body line below its box. The Training one came from the shared TrainingLoopBox, so its pill was removed at the component level and the takeaway re-added as body text in both lessons that use it (What Is AI? and Training). No copy changed beyond moving these lines out of the pills.

Update (2026-06-16): What Is AI? (aihistory) gained a closing TRY IT, "True or false?" (WHATQUIZ, quizAnswers state): seven True/False statements covering Training/Patterns/Probability/Prediction plus a few fun debunks (no humans typing in a data center, AI didn't grow up in Canada, read more than a person could in a thousand lifetimes), answers mixed T·F·F·T·T·F·F. The lesson was previously passive (NextLessonGate `ready: true`); Next now gates on `quizAllAnswered`.

Update (2026-06-10, back-half restructure): the 5-part content spine became 6 parts. Check the Output slimmed to the checking skills (opener, howmuchtocheck, critical, verify, evaluating). A new Avoid Traps section (id openerprotect for its opener; reworked from Protect Yourself) unites the three trap families in three opener movements: traps in the answer (hallucination, trainingbias, documenttrap), traps in you (mindtrap, flattery, engagementtrap, supporttrap), traps from the world (faketrap). Seeing Isn't Proof was renamed The Fake Trap (id seeingisntproof -> faketrap; the old title survives as its kicker). Build Your Advantage split into The Future (openerrealworld reworked; workchanges, agents, aijudges, aifuture) and Build Your Skills (NEW opener id openerskills; integrity, privacy, humanedge, creativethinking, buildedge), ending the course on Build Your Edge. Who Else Is Affected (stakeholders) was removed: its four-roles ShowcaseBox moved verbatim into When AI Judges You after the THREE MODES box; its trade-offs box, Spot the Cost activity, and KeyInsights are parked in docs/parking-lot.md. StakeholdersSection, WhenNotSection, and the retired openers remain dead code. Gates rewired: evaluating -> openerprotect -> hallucination; documenttrap -> mindtrap; supporttrap -> faketrap -> openerrealworld; privacy -> humanedge; aijudges -> aifuture -> openerskills -> integrity. Course total stays 61. Print packet slugs follow the new group labels.

Update (2026-06-08): Studying With AI was renamed Study with AI and moved from Protect Yourself into Start Smarter (placed after Why Bother Learning?), for its immediate value to students early in the course; Start Smarter went 7 → 8 and Protect Yourself 11 → 10. Choosing the Product and Choosing the Model were merged into a single lesson, Choose the App (surviving id `modelselection`); the former Choosing the Model component (`ChoosingModelSection`) is now rendered embedded as that lesson's second half (passed `embedded: true`, which suppresses its own header and Next button), and `choosemodel` was removed from `SECTION_GROUPS`/`SECTION_META`/`SECTION_COMPONENTS`. Work With AI went 10 → 9 and the course total 64 → 63. Course-wide, "product" was renamed to "app" wherever it refers to the AI app, so the three-decision framework is now App, Model, Mode; the hero illustration was renamed `illustrations/choosing-the-product.jpg` → `illustrations/choose-the-app.jpg` (same image kept) and still shows the old "Choosing the Product" slide title, so it (and its alt text) is pending a reshoot. Temperature was folded out of Work With AI into Inference (the last Understand AI lesson) as a closing beat: `TemperatureSection` now renders embedded (`embedded: true`) inside `InferenceSection` with prediction-anchored framing, and the standalone lesson's app/model/mode intro, prompting-tactics box, and Model/Mode KeyInsight were dropped; `temperature` was removed from `SECTION_GROUPS`/`SECTION_META`/`SECTION_COMPONENTS`, Thinking Mode now leads to Customization & Memory, and the Temperature glossary term plus two FAQ links repoint to `inference`. Work With AI went 9 → 8 and the course total 63 → 62; Understand AI stays 15 (Inference absorbed the content). The Thinking Mode lesson was renamed: id `thinkingmode` → `effort`, label "Thinking Mode" → "The Effort Dial", kicker "SPEED VS DEPTH" → "HOW HARD IT THINKS", and the three-decision framework is now App, Model, Effort (`ThinkingModeSection` keeps its function name; the id is the only registry change). Its body was reframed from the Direct/Thinking binary to an effort dial (cards "Quick answer" / "Deeper reasoning", quiz pills Quick/Deeper) to match how ChatGPT and Claude now expose reasoning effort; the Temperature glossary term plus two opener FAQ links repoint to `effort`, and stray "thinking mode" mentions in the Hallucination/Verify quizzes were reworded to "effort".

Update (2026-06-16): Study with AI (`studying`) was refocused on NotebookLM and its hands-on Lab ("Build Your Course Notebook"). The "Pick the Better Move" TRY IT (3 mint scenarios) was removed as redundant with the Best practices list, the tool-comparison box, and the Lab itself; its `picks`/`SCENARIOS`/`pickOption` are gone and the "Next: Foundations" gate is now `ready: true` (the Lab is take-home, so it can't gate). The cut scenarios are in `docs/parking-lot.md`. Section count unchanged.

Update (2026-06-17): Tokens (`tokens`) was reframed to motivate tokenization before naming it. The opener now shows a real chat exchange (static UserBubble/AIBubble on the "best Avengers movie" prompt) to make the humans-read-language vs. computers-read-numbers contrast concrete, then walks a failed naive fix (give every English word its own number → millions, and still can't cover unseen words) into the reusable-chunks idea, illustrated by a new "One chunk, thousands of words" ShowcaseBox (the `un` chip shared across unbelievable/unmatchable/untied). Only then are tokenization, tokens, and token ID named (the token-ID line now frames the ID as a meaningless address, bridging to Embeddings). The cat-vs-AI CompareBox and the "how text is split" examples are unchanged. The former "How the token vocabulary is built" section was cut down to a single line ("Once it’s built, the model uses that same fixed vocabulary..."); the per-model vocabulary sizes (ChatGPT ~200k, Gemini 256k, Claude not published) now sit inline in the tokenization paragraph instead. Copy/structure only; lesson map and counts unchanged.

Update (2026-06-17): Embeddings (`embeddings`) was reworked and the term **embedding vector** was deferred out of it. The intro after the Student-ID analogy now leads with the taste-test setup ("If someone asked you, 'Which drink has sweet of 9...'") and keeps only vector / dimension / value; the old "[9, 1, 10, 2, 3, 8] / order matters" line and the "embedding vector / starting meaning" paragraph were cut. The Taste Profile table gained a **Token ID** column (illustrative IDs >10k, muted gray) and a **Dimensions** group header over the six characteristic slots, with **Token / Token ID** column headings (top-level headers black/800, the line under Dimensions darkened); "Black Coffee" was renamed "Coffee" everywhere in Embeddings and Vector Space. Within Embeddings the lesson now says **vector** (cat→9246→vector box, the "Does every token get its own vector?" Q&A, and the Name That Drink activity). **embedding vector** is now first introduced in Black Box (`insidethemodel`), where the "five pieces" prose names it ("when one stands for a single token, pulled from the embedding table, it’s the token’s embedding vector") just before the MatchTerms checkpoint that tests it — so the term debuts where the embedding table and parameters do. Inference was unaffected (it already used "vector"/"starting meaning", not the term). Lesson map and counts unchanged.

Update (2026-06-17): the "Name That Drink" TRY IT in Embeddings was converted from sequential (Pattern 1: RevealSequence + mint QuizBlock, gated retry-until-correct) to parallel (Pattern 2: ScenarioRow + FeedbackPill, all three rows visible at once with per-option feedback; the trick "Not Enough Info" row keeps its per-pick feedback). The Pattern-1 `Takeaway` ("You read meaning straight from the numbers… that's exactly what a vector does for a token") was removed and re-homed as a `KeyInsight` that renders right after the activity once all three are answered (gated on `meaningComplete`). This activity was the source for `docs/components/try-it-takeaway.png` and the README's "TRY IT — completed, with Takeaway" example; since it no longer has a Takeaway, that screenshot was recaptured from "What Did the Model Actually Learn?" in Training Bias Trap (`trainingbias`, still sequential) and the README citation updated.

Update (2026-06-17, Understand AI "inside the model" reorder): the arc changed from `…tokens → embeddings → vectorspace → insidethemodel → attention → layers → training → prompt → inference` to `…tokens → embeddings → attention → layers → vectorspace → training → prompt → inference → insidethemodel`. Driving idea: Attention and Layers refine the embedded vector, so they follow Embeddings; Vector Space is the *payoff* of Layers' "Final Meaning" (a token's final point on the map), so it follows Layers; Black Box becomes the closing summary of the whole group. Black Box (`insidethemodel`) was un-overloaded by splitting "THE PIECES, INSIDE ONE BOX": (1) the `black-box-2.jpg` "Inside a real AI model" diagram + the embedding-table/embedding-vector/parameters naming moved to the **end of Embeddings** (an "INSIDE A REAL MODEL" capstone that replaced the simpler cat→9246→vector decimal box — so embedding table/vector/parameters now debut earliest, before everything that uses them); (2) the deep "70 billion parameters" paragraph was first moved to **Training**, then deleted entirely (2026-06-17) as redundant — parameters are now introduced in the Embeddings "INSIDE A REAL MODEL" capstone, and Training only touches them via "weights, also called its parameters" in the loop card (Training's old "you met in Black Box" ref was reworded to "also called its parameters" during the reorder, since Black Box is now after it); (3) the "Name the Piece" MatchTerms checkpoint + the learned-numbers KeyInsight stay in Black Box, which is now a pure end-of-group recap. Content surgery on the Layers↔Vector Space seam (final state 2026-06-17): split cleanly by job, with the bank example as the bridge. **Layers** is now purely the *process* and no longer has a "Final Meaning" section at all — after "Why so many layers?" it ends on the "representation, not an answer" KeyInsight. Its only illustration is `layers-1.jpg` (the inside-one-layer process diagram); the `layers-2.jpg` bank-journey board-game map was *moved to Vector Space* because it's really a vector-space picture (a word traveling past meaning-neighborhoods — money/vending/blood/river bank — and landing on its final position). **Vector Space** owns the product and now *opens on the bank* ("You just watched 'bank' travel through the layers and settle into one final vector… it's a **position**, a single point in a vast space of meaning"), then "where a point sits is what it means / similar meanings land close," then the `layers-2.jpg` map as the opening visual ("Here's that idea in one picture…"). Note: Vector Space now carries two map illustrations — `layers-2.jpg` (one word's disambiguation, opener) and `vector-space.jpg` (cross-word clustering: sodas/coffees/animals/vehicles, later); they show different facets (disambiguation vs clustering). The Coke/Pepsi/Coffee table is kept and reframed as the *measurement* demo (bridge: "the bank's final vector is real numbers, just ones we can't read; to make 'close' concrete, go back to numbers you can" — the taste test): it shows distance = sum of gaps and the "six dimensions can't separate Coke from Pepsi; add Citrus and they split" point, i.e. *why* real models need thousands of dimensions. Closes on prediction ("prediction itself is a map move" — Inference's SEE IT references "the meaning map from Vector Space"). Its taste-profile table was also reformatted to match the Embeddings table (Token / Token ID columns, Dimensions group header; Coke=24317, Coffee=51820 kept consistent with Embeddings, Pepsi=38106). Principle: Layers = how the numbers change; Vector Space = the final vector is a position in a space, and what that buys (similarity, distance, prediction). Eliminating Vector Space (folding into Layers) was considered and rejected because the map concept is load-bearing for Inference.

Update (2026-06-17, supersedes the split above — Vector Space merged into Layers): after repeatedly fighting the seam, Vector Space was merged in as Layers' closing section, dropping the standalone lesson. Mechanism: `VectorSpaceSection` now takes an `embedded` prop (suppresses its own `LessonHeader`/`LessonRule`/`NextLessonGate`) and is rendered at the end of `LayersSection` via `E(VectorSpaceSection, { embedded: true })`, followed by a new final KeyInsight ("By the last layer, meaning is a position… the next word it picks is whatever sits closest") and `Next: Training`. Trims taken in the merge: cut the `vector-space.jpg` neighborhoods illustration (the `layers-2.jpg` bank map is the one map now), the "Think of vector space like a city map" analogy, and the "close ≠ identical" aside; the prediction paragraph was dropped entirely (its point now lives in the final KeyInsight). The embedded section was then reorganized concrete-first (clearer than the old abstract-thesis opener): VECTOR SPACE kicker → "Let's go back to your taste test" → the Coke/Pepsi/Coffee table → "Coke and Pepsi sit closer than Coffee" → **distance** (the measure) → "Now back to bank: running the layers changed its vector into a brand-new row of numbers" → the catch ("that new set doesn't match any single word's vector; it's one of a kind") → "the idea of **vector space**: a map where similar words share a neighborhood" → the `layers-2.jpg` illustration as the payoff. `vectorspace` was removed from `SECTION_GROUPS`/`SECTION_META`/`SECTION_COMPONENTS`/`CHIP_LABELS` (the `VectorSpaceSection` function stays, used only embedded). Downstream: Inference's SEE IT now says "the meaning map from **Layers**" (was Vector Space); the Build-Your-Skills recap dropped "vector space" from "tokens, embeddings, layers"; a Test Yourself question's `lessonId` repointed `vectorspace → layers`. Understand AI 14 → 13; course total drops by 1. Layers' SECTION_META label was deliberately kept as "Layers" (the lesson still leads with layers and the onion icon fits; kicker "BUILDING UP MEANING" is vestigial/unrendered). NextLessonGate targets rewired accordingly (embeddings→attention, layers→vectorspace, vectorspace→training, inference→insidethemodel, insidethemodel→openerworkwith). Lesson map order changed; counts unchanged (Understand AI still 14, course total unchanged). This re-homes "embedding vector" into Embeddings' capstone, consistent with the earlier deferral (the lesson *body* still teaches plain "vector"; only the closing capstone names it).

Update (2026-06-18): the Inference lesson's opening flow box (InferenceFlowDiagram) and the "The Complete Journey" step-through were both replaced by a single static InferenceJourneyDiagram. Rebuilt from a Claude Design handoff into two phase lanes matching the purple/amber loop-box convention: Phase 1 "Read the message" (purple) holds Context Window, Tokens, Embeddings, Transformer (the emphasized "The core" card: attention then transformation across ~100 layers); a milestone bar bridges to Phase 2 "Write the reply" (amber) with Probability, Prediction, Output, Loop. Each lane uses a numbered spine (1-8) rather than repeated arrows. Variant A return-loop: an L-shaped rail in a reserved right gutter (RAIL_W 96px) runs the full height back to the top ("no memory · the whole journey runs again"); on narrow widths (<640px) the rail is dropped for a caption under the last step. The diagram carries its own lead paragraph (the prior BodyP lead-in was removed). Colors/spacing/type are the handoff's exact values; phase palette is raw hex (course ink tokens reused for text). The diagram copy is deliberately em-dash-free (the handoff had 3; kept out per course style).

Update (2026-06-18): the "Same question, different prediction" SEE IT (the Luke/Nate car demo), its following illustration (inference-2.jpg), and the now-redundant "One answer. Many picks." KeyInsight were moved out of Inference into Context Window (prompt), placed right before that lesson's "Same question, different answer. The context window is the difference." KeyInsight. Rationale: the demo's whole point is that the context window changes the output (it fits Context Window, which already tells the Luke/Nate story in prose), and Inference was carrying too many moving pieces while the demo reinforced only a couple. The demo's state (seeStep, the promptChars/aiResp* typing animations) now lives in PromptSection. Gating moved with it: Context Window's NextLessonGate now requires completing the demo (seeStep >= 6), and Inference's gate now keys on the "ChatGPT, decoded" reveal being fully opened (Object.keys(revealed).length >= COLS.length).

Update (2026-06-18): Temperature was moved out of Inference into Choose the Model (`choosemodel`), reversing the 2026-06-08 fold-into-Inference (which is also now stale on the merge: `choosemodel` is a live standalone lesson again, flowing `modelselection` → `choosemodel` → `effort`). The embedded temperature beat was removed from `InferenceSection` (the `TemperatureSection({ embedded: true })` render is gone; Inference now goes straight from the neural-net image to the "ChatGPT, decoded" closer). `TemperatureSection` gained an `inline` prop — a third mode beside standalone/embedded: it renders a "One more dial: temperature" `SectionKicker` instead of a `LessonHeader`, a model-page-anchored intro ("Picking the model sets how much brain you get… temperature"), the temperature-only KeyInsight (not the four-part App/Model/Effort/Verification one), and no trailing `LessonRule`/`NextLessonGate`. It's rendered via `E(TemperatureSection, { inline: true })` at the end of `ChoosingModelSection`, after the free-plan caveat and before that lesson's `Next: Effort Dial` gate. Cross-refs repointed: the Effort lesson's "you met one dial already: temperature" now reads "back on the model page" (was "back in Inference"), and the Temperature glossary term's `source` is now `choosemodel` (was `inference`). The standalone/embedded `TemperatureSection` branches remain defined (embedded is now dead code — no caller passes `embedded: true`). Lesson map and counts unchanged (temperature is still not its own section node). Separately, copy-only: the Probability step body in `InferenceJourneyDiagram` was reworded to drop "the last token's final vector."

Update (2026-06-18): the Black Box lesson (`insidethemodel`, `BehindTheNumbersSection`) was dissolved — it was a thin "black box" paragraph plus a vocab checkpoint, and the black-box *concept* was already taught downstream in The Future. Its pieces were redistributed: (1) the three black-box paragraphs + the `black-box-1.jpg` illustration moved into The Future (`AIFutureSection`), where the "WHY OUTSIDE EVIDENCE MATTERS" beat was renamed `SectionKicker` "AI IS A BLACK BOX" and now opens with that prose; the old recap line "Earlier in the course you saw why AI is a black box: …" was rewritten to "That opacity is exactly why future claims are hard to check. …" (the existing `BLACK_BOX_REASONS` 4-card grid still follows). (2) The `MatchTermsTryIt` "Name the Piece" checkpoint + its "You've now met every piece…" lead moved to the **end of Inference** under a new "LOCK IN THE PIECES" `SectionKicker`, becoming the Understand AI section's closing lock-in; `InferenceSection` gained `matchComplete` state and its `NextLessonGate` now keys on it and targets `openerworkwith` ("Next: Work With AI", was → `insidethemodel` "Next: Black Box"). (3) The "HOW BIG THIS GETS IN A REAL MODEL" beat and the trailing "learned numbers, not written rules" `KeyInsight` were dropped. `insidethemodel` was removed from `SECTION_GROUPS` and `SECTION_COMPONENTS`; its `SECTION_META` entry was left as inert vestigial (like retired `openerinside`'s). `BehindTheNumbersSection` stays defined as dead/unreferenced code (house pattern). Understand AI 13 → 12; course total drops by 1. The Understand-AI opener bridge that promises "AI gets called a 'black box.' You're about to open it…" was left as-is (still true; the opacity nod now pays off in The Future).

Update (2026-06-18): the Attention lesson (`HowAIReadsSection`) was renamed **Transformer** and now names the architecture by name (it previously taught attention + transformation without ever saying "Transformer," which the "Name the Piece" quiz had started testing). Label-only rename: `SECTION_META.attention.label` "Attention" → "Transformer"; the id stays `attention`, so registries, gate targets, `lessonId` refs, and the Attention/Transformation glossary sources are unchanged. Body framing makes the Transformer the architecture that spans **this lesson and Layers** (matching the Inference journey diagram's "Transformer = attention then transformation across ~100 layers"): at "THE BREAKTHROUGH," the 2017 *Attention Is All You Need* line now adds "It introduced the **Transformer**, the architecture behind every modern LLM, and the 'T' in ChatGPT"; the impact line became "the Transformer reads your whole text at once"; a lead-in "It makes sense of all those words with two moves:" precedes the Attention/Transformation cards; and the closing `KeyInsight` ("It made AI possible.") now reads "Attention then transformation: together they're the **Transformer** … runs those two moves over and over, stacked in **layers**, which is next" (handoff to Layers). The Embeddings `NextLessonGate` label was updated "Next: Attention" → "Next: Transformer" (target still `attention`). The following **Layers** lesson was updated to land the handoff: its intro now reads "Each pass is called a layer, and the Transformer stacks dozens of them. At every layer it runs the same two moves you just met: attention … transformation," and its stale callback "(the pronoun example from Attention)" was repointed to "from Transformer." Lesson map and counts unchanged.

Update (2026-06-18): new lesson **The Hidden Cost** (`computecost`, `TheHiddenCostSection`) added to **Embrace the Future**, between AI as Judge (`aijudges`) and The Future (`aifuture`). Topic: AI's compute + energy/environmental footprint, which nothing else covered (only a single line in Choose the Model touched it). Arc: a mundane chat (reused `UserBubble`/`AIBubble`: "name for a golden retriever puppy") → a gated SEE IT ("What one answer costs") that reveals "~40 trillion calculations" with a breakdown that *earns* the number (~50 tokens × ~400B parameters × 2 operations, tied back to tokens/parameters and the Inference loop) → a ShowcaseBox metaphor ("40 trillion dollar bills end to end reach past Pluto, ~6 billion km") with an Earth→Pluto bar and an "illustrative, varies by model/length" disclaimer → "WHY DATA CENTERS EXIST" (GPUs, 24/7) → "THE FOOTPRINT" (electricity + water, growing, but normalized — every big tech has one, efficiency improving) → a `Takeaway` ("Now you know what's behind the magic"). Deliberately **awareness, not a behavior ask**: no "use less"/green-CTA (audience is high-schoolers we want comfortable using AI). Wiring: added to `SECTION_GROUPS`/`SECTION_META`/`SECTION_COMPONENTS`; `aijudges`'s two `NextLessonGate`s now target `computecost` ("Next: The Hidden Cost", were → `aifuture`); the new lesson's gate (ready on the SEE IT reveal) → `aifuture` ("Next: The Future"). Embrace the Future 5 → 6 lessons; course total +1. (Related future work the user raised: splitting `aifuture` into "Judging AI's Future" + "Talking About AI" — done in the next update.)

Update (2026-06-18): **The Future** lesson was split in two — it held ~2 lessons of material. **Lesson A keeps id `aifuture`, retitled "Judging AI's Future"** (`SECTION_META.aifuture.label`/`title` "The Future" → "Judging AI's Future"): AGI/ASI/ANI vocab + "Why Smart Predictions Fail" (the predictions reveal + friction) + the three tests + the black-box/outside-evidence beat, ending on "a confident explanation isn't evidence" plus a one-line bridge to the next lesson. **Lesson B is new: "Talking About AI" (`talkingai`, `TalkingAboutAISection`)** — the "TALKING ABOUT AI" ShowcaseBox (the four worries: takeover / jobs / hype / cheating, each with the concern underneath) and the "the smartest people … can judge" closer were moved out of `aifuture` into it, behind a short bridge intro ("you'll also be the person friends, family, and teachers ask about AI"). Gate chain rewired: `computecost` → `aifuture` ("Next: Judging AI's Future") → `talkingai` ("Next: Talking About AI") → `openerskills` ("Next: Build Your Skills"); `talkingai`'s gate is `ready: true` (no interactive). Added to `SECTION_GROUPS`/`SECTION_META`/`SECTION_COMPONENTS`. Embrace the Future 6 → 7 lessons; course total +1.

Update (2026-06-18): **lesson gating softened from a hard wall to a nudge** (course-wide behavior change, intentional — don't "fix" it back). `NextLessonGate` now *always* renders the clickable `NextCardUnlocked` regardless of `ready`; when the activity isn't complete it shows an amber hint ("You can keep going, but the activity above is the point.") via a new `hint` prop instead of the old non-clickable `NextCardLocked` dead-end. Rationale: the top `ChipNav` was always free, so the bottom hard-lock was a half-gate that added friction without enforcing anything; for a self-paced, agency-first, ungraded course it's on the student to finish activities. Each section still passes its `ready` condition (drives whether the hint shows), and clicking Next still runs the section's `onClick` (`completeAndNavigate`). `NextCardLocked` is now unused/dead code; the top nav is unchanged.

Update (2026-06-22): the Embeddings (`embeddings`) Token/Token ID/Citrus-dimension showcase boxes were revised and the lesson got a real closing TRY IT. (1) The two static taste-profile boxes are now **Coke vs. Coffee** (six dims) and **Coke vs. Pepsi vs. Coffee** (seven dims, with **Citrus** bolded in the header) — the second box previously showed only Coke vs. Pepsi; Coffee was brought back to match the Layers/Vector-Space table, and the "What about Pepsi?" prose ("To separate them, you added a new dimension, Citrus") was tightened to past tense. (2) New **"Build a Club Vector"** TRY IT (mint `InteractiveBox`) added as the lesson's closer, after the "Does every token get its own vector?" beat and before `LessonRule`/`NextLessonGate`. The student scores three clubs (🧱 Lego, 🤖 AI, 🍖 BBQ) 0–10 across five dimensions (Tech, Build Things, Messy, Tasty, Career Edge) in a 15-cell grid; "Hide the names and match" locks the inputs, **collapses the entry table** (so the names aren't sitting in view during matching), and reveals three anonymized "Mystery Club A/B/C" rows (numbers only) that the student matches back to a club via `ScenarioRow` + `FeedbackPill`. In the match phase an "Expand table / Collapse table" toggle (`clubTableShown`) lets the student peek at the locked read-only grid. A "tie" branch fires when two clubs are scored identically ("too similar to tell apart" — the Coke/Pepsi/Citrus callback). It ends on a `Takeaway` ("The numbers carried the meaning.") and the lesson's `NextLessonGate` now keys `ready` on `clubMatchComplete` (was `ready: true`); per the 2026-06-18 soft-gate, this drives the nudge hint, not a hard lock. The box uses the house `lead` pattern (priming paragraph passed as `InteractiveBox` `lead`, so a single "TRY IT" `ActivityLead` kicker renders above it and the box's own eyebrow is suppressed). State lives in `EmbeddingsSection` (`clubVals`/`clubLocked`/`clubOrder`/`clubPicks`/`clubTableShown`). This supersedes the older "Name That Drink" activity as the Embeddings TRY IT: `NameThatDrinkTryIt` and the `meaningComplete` state in `EmbeddingsSection` are now unreferenced/dead code (house pattern). Lesson map and counts unchanged.

Update (2026-06-25): **How We Got Here (`howwegothere`) was moved out of Understand AI → Foundations to the end of Finish Smarter, after Beat the Clock (`headtohead`)** — parked off the spine (the user's "essentially the parking lot for the lesson"). It's removed from Understand AI's `SECTION_GROUPS` list and appended last in Finish Smarter's, so it stays a live, nav-reachable lesson (`SECTION_META`/`SECTION_COMPONENTS`/`HowWeGotHereSection`/video manifest all unchanged) but is no longer auto-routed into. Wiring: the Foundations opener (`openerfoundations`) now goes `nextLessonId` `howwegothere → aivscode` ("Next: How We Got Here" → "Next: Rules vs Patterns"), so Foundations opens straight on Rules vs Patterns; How We Got Here's own bottom `NextLessonGate` was repointed from `aivscode` to a terminal `welcome` ("🗺️ Back to Start") since it now sits at the very end (its Previous card auto-derives to Beat the Clock from nav order). Beat the Clock is untouched — it stays the climax with its Run it again / Back to Start results screen and is NOT chained into How We Got Here. Two broken cross-references were fixed: (1) the Foundations opener's `groups` preview dropped its first cluster ("WHERE AI CAME FROM", which previewed How We Got Here) and its `sectionOverview` subtitle went "Five moves…" → "Four moves…"; (2) the AI is Math (`aiismath`) opener line "You met these ideas by name on the timeline in How We Got Here" was repointed to "back in What Is AI?" (where the four ideas / CoreLoopBox actually debut, in Start Smarter), since a student now reaches AI is Math before ever seeing How We Got Here. Understand AI 13 → 12 (Foundations 5 → 4), Finish Smarter 5 → 6; course total unchanged (62).

Update (2026-06-25, spine reorder): **Work With AI was moved ahead of Understand AI**, so the spine is now Start Smarter → Work With AI → Understand AI → Check the Output → … (was Start Smarter → Understand AI → Work With AI → …). The course now teaches *use it* before *understand how it works*. Just the two blocks swapped in `SECTION_GROUPS` (no lesson moved between groups; counts/badges derive automatically — Work With AI is now section 2 of 8, Understand AI section 3). Seam gates rewired: `whybother` → `openerworkwith` ("Next: Work With AI"), `thoughtpartner` → `openerfoundations` ("Next: Foundations"), `inference` → `openercheck` ("Next: Check the Output"). Cross-references that assumed the old order were fixed: four explicit forward-callbacks in Work With AI lessons were restated as inline facts rather than "you already learned X" (modelselection "Back in Training you learned the model is frozen" → "An AI model is frozen once it's trained"; customization "Remember the lesson on Context Window?" → "Every model has a context window: …"; prompting "You met this in the Patterns section" → "LLMs run on patterns"; prompting "As you learned in the Context section" → "The model can only work with what's in its context window"). The two expandable "mechanism" detail cards in What AI Does Best (`whatitdoesbest`) were de-jargoned (dropped embeddings/attention/token/probability/temperature vocab not yet taught). Both section openers were reframed for the new sequence: Work With AI's "You know how AI builds an answer. Now…" → "You've met the tool and seen what it can do. Now…"; the Foundations opener was rewritten from a "fundamentals come first / you can't use a tool until you understand it" promise (with a now-backwards basketball "fundamentals-first" analogy) to a "you've been using AI; now open it up" framing with a car/engine analogy ("you can drive without opening the hood, but the driver who knows the engine reads trouble early") that echoes Meet the Tool's engine-under-the-hood line. Confirmed false positives left untouched: the Context Window (`prompt`) lesson's refs to Tokens/the calculator/"Chinese Room from Does AI Think?" (all still earlier — intra-Understand-AI order preserved, Does AI Think? is in Start Smarter), and effort's "temperature, back on the model page" (Choose the Model stays before it in Work With AI). Course total unchanged (62).

Update (2026-06-25): **Rules vs Patterns (`aivscode`) and Harder to Control (`norules`) were merged into one lesson, "AI is Different" (surviving id `aivscode`)** — a raw full-content combine for now (the user will trim/move/cut later). Mechanism: `BlackBoxSection` (the function behind `norules` — name is a vestigial quirk) gained an `embedded` prop that swaps its `LessonHeader` for a large "NOBODY PROGRAMMED THIS" `SectionKicker` seam (its old kicker) and otherwise renders its full body plus its existing bottom gate. `AIvsCodeSection` now drops its own `LessonRule` + "Next: Harder to Control" gate and renders `E(BlackBoxSection, { embedded: true, ...nav props })` right after the "Which One Handles This?" quiz, so the lesson reads: AI-is-Different header → all Rules-vs-Patterns content → NOBODY PROGRAMMED THIS seam → all Harder-to-Control content → one "Next: AI is Math" gate (the embedded section's gate, `ready` on its scenarios; `aivscode`'s old `quizDone` gate is now dead state). `norules` removed from `SECTION_GROUPS` and `SECTION_COMPONENTS`; `SECTION_META.aivscode` label "Rules vs Patterns" → "AI is Different" (kicker "TWO WAYS TO BUILD SOFTWARE" left vestigial). Cross-refs repointed `norules` → `aivscode`: the **Guardrail** glossary `source` and a **Test Yourself** question's `review` (its `section` label "Harder to Control" → "AI is Different"). Left inert/vestigial: `SECTION_META.norules`, the unused `Harder_to_Control.mp4` video-manifest entry, and the `sectionId: "norules"` `LessonHeader` branch inside `BlackBoxSection` (only the embedded path runs now). Understand AI 12 → 11 (Foundations 4 → 3); course total 62 → 61.

Update (2026-06-25): **AI is Different (`aivscode`) moved from Understand AI → Foundations into Work With AI, as the second lesson (right after the opener `openerworkwith`, before What AI Does Best).** Rationale: ground the section in "AI runs on patterns, not rules" before the practical app/model/prompt choices. It moves earlier in the course (Work With AI is section 2, Understand AI section 3) but still sits after Start Smarter and before all the Understand AI mechanics (tokens/embeddings/training), so no content cross-references break — the lesson body is self-contained ("Traditional software uses rules… AI is different… AI is like a chef"). Wiring: removed from Understand AI's `SECTION_GROUPS` list, inserted after `openerworkwith` in Work With AI's. Gates rewired: `openerworkwith` → `aivscode` ("Next: AI is Different", was → `whatitdoesbest`); `aivscode`'s bottom gate (the embedded section's) → `whatitdoesbest` ("Next: What AI Does Best", was → `aiismath`); `openerfoundations` → `aiismath` ("Next: AI is Math", replacing the stale "Next: Rules vs Patterns" label that pointed at `aivscode`), so Understand AI now opens straight on AI is Math. The Foundations opener's preview lost its "NO ONE WROTE THE RULES" cluster (it previewed the departed lesson) and its `sectionOverview` subtitle went "Four moves…" → "Three moves…". Left as-is (thematic, not lesson-by-lesson, so not broken): the Work With AI opener's three-part preview (KNOW WHAT IT'S FOR / PICK THE TOOL / USE IT WELL). Work With AI 9 → 10, Understand AI 11 → 10 (Foundations 3 → 2); course total unchanged (61).

Update (2026-06-25): **Customization & Memory (`customization`) was merged into Context Window (`prompt`, in Understand AI), which keeps the name "Context Window".** Full-content combine (edit later), same `embedded` pattern as the earlier merges: `CustomizationSection` gained an `embedded` prop that swaps its `LessonHeader` for a large "MAKE IT YOURS" `SectionKicker` seam and suppresses its trailing `LessonRule` + "Next: Questions Matter" gate; `PromptSection` renders `E(CustomizationSection, { embedded: true, ...nav props })` right after its last activity (`PromptSplitTryIt`) and before its own `LessonRule` + "Next: Inference" gate. So Context Window now reads: header → all Context Window content → MAKE IT YOURS seam → all Customization content → one "Next: Inference" gate. This pulls Customization *later* in the course (out of Work With AI section 2 into Understand AI section 3); nothing references it that breaks (no concept callbacks in the following Work With AI lessons; no video manifest entry). `customization` removed from `SECTION_GROUPS` (Work With AI) and `SECTION_COMPONENTS`; `SECTION_META.customization` left vestigial. Gate rewired: `effort` (ThinkingModeSection) → `questionsvaluable` ("Next: Questions Matter", was → `customization`). Cross-refs repointed `customization` → `prompt`: the **System Prompt** glossary `source` and a **Test Yourself** question's `lessonId`. Left for a later edit (redundant, not broken): Customization's opening line "Every model has a context window: the working memory it uses to answer your prompt" now sits inside the Context Window lesson, and Context Window's "Next: how it turns all of it into an answer, one word at a time" bridge now precedes the appended Customization content rather than leading straight into Inference. Pre-existing dead code left as-is: `TemperatureSection`'s standalone `NextLessonGate` (never rendered — Temperature is always `embedded`/`inline`) still targets `customization`. Work With AI 10 → 9; Understand AI unchanged (Context Window stays); course total 61 → 60.

Update (2026-06-25): **Critical Thinking (`critical`) moved from Check the Output into Build Your Skills, placed before Creative Thinking (`creativethinking`), after Skills That Matter (`humanedge`).** It stays a live standalone lesson (not merged) — `SECTION_META`/`SECTION_COMPONENTS`/`CHIP_LABELS`/Test-Yourself refs all unchanged; only its `SECTION_GROUPS` membership and three gates moved. Gates rewired: How Much to Check (`AIStrengthsSection`/`howmuchtocheck`) → `verify` ("Next: Verify", was → `critical`), so the checking cluster is now opener → How Much to Check → Verify → Evaluate; Critical Thinking → `creativethinking` ("Next: Creative Thinking", was → `verify`); Skills That Matter (`humanedge`) → `critical` ("Next: Critical Thinking", was → `creativethinking`). One content reword: Verify's opener "Critical thinking helps you spot a weak claim anywhere" → "Questioning an answer helps you spot a weak claim anywhere" (Critical Thinking now comes after Verify, so the old phrasing was a forward reference). Left as-is (still accurate): Verify's "You already learned to match the check to the task in How Much to Check" (How Much to Check still immediately precedes Verify); both section openers' thematic preview clusters (Check the Output's "QUESTION THE ANSWER" still covers Verify+Evaluate; Build Your Skills' "YOUR EDGE" still covers Critical Thinking); and Critical Thinking's own self-contained opener. Check the Output 5 → 4, Build Your Skills 7 → 8; course total unchanged (60).

Update (2026-06-25): **Verify (`verify`) was merged into Evaluate (`evaluating`), which is relabeled "Evaluate Results".** Full-content combine (edit later), same `embedded` pattern: `VerifySection` gained an `embedded` prop that swaps its `LessonHeader` for a large "TRUST, THEN CHECK" `SectionKicker` seam and suppresses its trailing `LessonRule` + "Next: Evaluate" gate; `EvaluatingSection` renders `E(VerifySection, { embedded: true, ...nav props })` after its last activity (`GoodEnoughTryIt`) and before its own `LessonRule` + "Next: Avoid Traps" gate. So Evaluate Results reads: header → Evaluate content → TRUST, THEN CHECK seam → Verify content → "Next: Avoid Traps" gate. `verify` removed from `SECTION_GROUPS` (Check the Output) and `SECTION_COMPONENTS`; `SECTION_META.verify` left vestigial; `SECTION_META.evaluating` label "Evaluate" → "Evaluate Results". Gate rewired: How Much to Check (`AIStrengthsSection`/`howmuchtocheck`) → `evaluating` ("Next: Evaluate Results", was → `verify`). Cross-refs repointed `verify` → `evaluating`: one Test Yourself `lessonId` and two `review` links. Left as-is (descriptive, not nav): the Full Loop (`fullworkflow`) workflow-deck card `{ id: "verify", section: "Verify" }` and `WORKFLOW_DECK_ORDER`'s "verify" step. Flagged for a later edit (not broken): (1) Evaluate's intro "You've learned how to verify whether AI output is true" now sits before the appended Verify content it references, and (2) the merged order is Evaluate-then-Verify — reverse of the natural verify-then-evaluate (the workflow deck even notes "Verify comes BEFORE evaluating"); flip if desired. Check the Output 4 → 3; course total 60 → 59.

