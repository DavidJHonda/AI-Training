SYSTEM INSTRUCTION: You must strictly adhere to all visual, narration, pacing, and containment rules in your source document named "Master Prompt". Do NOT read those guidelines aloud. Scene instructions below take precedence over any general rule in that document.

CRITICAL CONSTRAINTS (STRICT):
1. NO TITLE CARD: Never render the lesson title (or any title) as an opening screen or pill. Open directly on Scene 1’s first visual; the narrator does NOT say "welcome" and does NOT read the title aloud.
2. NO SCREENSHOTS / NO PHOTOS: Never screenshot paragraphs, text blocks, tables, or UI boxes; never show the lesson document on screen; draw every other visual fresh. Only exception: the five attached boards named below.
3. ONLY THE SCENES LISTED: no extra b-roll, interstitial, chapter-title, numbered-section-card, or decorative scenes. All on-screen text comes verbatim from the scene list or the named boards; never render annotation-style scribble text.
4. BOARDS: every board scene reproduces the named attached image exactly — every word crisp, full-bleed, held static, never redrawn, panned, cropped, or restyled; add NO text beyond what it carries. Each spoke board appears in exactly ONE scene.
5. NUMBERS: the ONLY numbers ever on screen are those on "how-an-llm-works-odds.jpg"; never render any other percentage, decimal, counter, or score; never draw your own chart, graph, or bar.
6. THE MAP RETURNS: "how-an-llm-works-coreloop.jpg" returns after every idea — the IDENTICAL board every visit; the ONLY change is which capsule is highlighted.
7. RUNTIME: target 3.5 to 4 minutes.

FOCUS: Analyze the attached file "how-an-llm-works.md" verbatim.
TOPIC: How an LLM Works (Four Ideas, One Example)

🎬 SCENE BREAKDOWN:
- SCENE 1: The Hook. (the video’s FIRST visual — no title card before it). Sketch a hand holding a phone mid-text, three blank suggestion chips above the keyboard, watercolor accent. Write NO text inside the drawing. One black-ink label: "Not looking up meaning — predicting the next word."
- SCENE 2: The Map. (HARD CANVAS WIPE). Board "how-an-llm-works-coreloop.jpg"; narration walks the two phases — Learn once, then Answer every word. End by highlighting ONLY the "🌱 Training" capsule.
- SCENE 3: Training. (HARD CANVAS WIPE). Board "how-an-llm-works-training.jpg"; narration walks the three cards left to right. Then (HARD CANVAS WIPE) the identical coreloop board returns, highlight now on "🍀 Patterns".
- SCENE 4: Patterns. (HARD CANVAS WIPE). Board "how-an-llm-works-patterns.jpg"; narration walks its three lines. Then (HARD CANVAS WIPE) the identical coreloop board returns, highlight crossing the center arrow to "🎲 Probability".
- SCENE 5: Probability. (HARD CANVAS WIPE). Board "how-an-llm-works-odds.jpg"; narration walks the left panel, then the right. Then (HARD CANVAS WIPE) the identical coreloop board returns one last time, highlight on "✍️ Prediction".
- SCENE 6: Prediction. (HARD CANVAS WIPE). Board "how-an-llm-works-prediction.jpg"; narration walks the three rows top to bottom as the sentence grows.
- SCENE 7: Three Myths. (HARD CANVAS WIPE). Three stickies crossed out with magenta X's, revealed with narration, EXACT words: "Magic", "A person", "A truth machine". Close with a dark pill, EXACT words — the FINAL visual, nothing follows it: "Math, predicting the next word."

🗣️ NARRATIVE ARC:
Hook: your phone suggests the next word as you text. ChatGPT and Claude do the same at enormous scale — not looking up what your words mean, running math to predict the likely next words. The map is the whole lesson: learn ONCE from mountains of text; then, every chat, use those patterns to build the answer one word at a time. Four ideas, one peanut-butter example through all four. Training: the model teaches itself — reads more than you could in a thousand lifetimes, guesses the next word, gets cloud wrong, nudges its numbers toward jelly. Billions of passes; that's training. Back to the map: training left patterns you picked up as a child: peanut butter and — jelly. You knew it; so does AI. Twinkle twinkle little star, once upon a time, better late than never. Not memorized; absorbed from billions of examples. Back to the map: patterns power every answer, starting with probability. It scores EVERY possible next word, and the surrounding text shifts the scores: after "peanut butter and", jelly leads; add banana and jelly crashes while sandwich takes over. One word changed everything. Map, one last time: prediction. An answer is hundreds of words, so it repeats the move — predict, look again, predict the next — no fixed plan for where the sentence ends. Fast enough, it looks like thought. Close on the three myths: not magic — math working out probabilities. Not a person — no thoughts, no understanding, even when it sounds like both. Not a truth machine — it predicts what sounds likely, so a wrong answer sounds as confident as a right one. Keep those straight and much of the confusion falls away.
