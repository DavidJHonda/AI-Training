Create a video overview of the attached lesson "Embeddings," about 3 minutes long. The attached embeddings.md is the full lesson text; the numbered images are the lesson's key boxes, in order — show each one as the narration reaches it. Focus: how a token ID becomes meaning. Open on the leftover problem from the last lesson: a token ID is just a number, like a student ID — it might get you in the building, but it says nothing about who you are. AI's way of turning token IDs into meaning is called embedding. Build the taste test: you and your friends rate Coke and a cup of coffee on six characteristics — image 1, the two-drink table: ask which drink has sweet 9, bitter 1, and fizz 10, and answer Coke in the same breath — by rating the drinks you represented a word's meaning as a row of numbers. Name the three terms: the row is a vector, each slot is a dimension, each number is a value. Then the Pepsi problem — image 2, the three-drink table: Pepsi's first six numbers match Coke's exactly, and only the new Citrus dimension finally tells them apart — different numbers, different meanings. Real models can't add a hand-labeled dimension for every difference in the language, so every token instead gets thousands of dimensions, learned during training. Make the easy-to-miss point: every token is scored on the SAME dimensions — map and truck get Sweet and Fizz scores too, sitting near zero — only the values change from token to token, and that's what makes any two rows comparable. Close the loop inside a real model: cat's token ID 9246 looks up its row in the embedding table; that row is cat's embedding vector, and the numbers filling the table are parameters, with dimensions the model chose itself during training. Every token gets its own vector — un, believ, and able each get one — and combining them into the whole word's meaning happens later, in the layers. End on the attached close image — the final thing on screen.
Rules:
1. No photographic or stock imagery of any kind — no Getty or watermarked images.
2. Never show the lesson document itself as a page on screen; show the attached images or draw fresh visuals.
3. Show the attached images exactly as provided — never redraw, restyle, or replace them with your own version.
4. Never place white or light-colored text on a light background; all text and labels must be dark ink on light backgrounds.
5. Do not invent numbers, percentages, or statistics — use only values that appear in the lesson.
6. The narrator never asks the viewer to pause, guess, or answer; answer any question the lesson poses in the same breath.
7. The close image is the final frame of the video — nothing plays after it, no outro or summary scene.