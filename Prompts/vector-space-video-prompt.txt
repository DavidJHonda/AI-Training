SYSTEM INSTRUCTION: You must strictly adhere to all visual, narration, pacing, and containment rules in your source document named "Master Prompt". Do NOT read those guidelines aloud. Scene instructions below take precedence over any general rule in that document.

CRITICAL CONSTRAINTS (STRICT):
1. NO TITLE CARD: Never render the lesson title (or any title) as an opening screen or pill. Open directly on Scene 1's first visual on the light grid-paper canvas; that first frame sets the style. The narrator does NOT say "welcome" and does NOT read the title aloud; narration begins directly with the hook.
2. NO SCREENSHOTS / NO PHOTOS: Never screenshot paragraphs, text blocks, tables, or UI boxes, and never show the lesson document itself on screen. Draw every visual fresh. No photographic or stock imagery of any kind.
3. ONLY THE SCENES LISTED: Render exactly the scenes below — no extra b-roll, interstitial, chapter-title, or decorative scenes. All on-screen text comes verbatim from the scene list; never render annotation-style scribble text or invented statistics.
4. NUMBERS: all values on screen come from the attached boards; invent none.
5. RUNTIME: target 3 to 3.5 minutes.

FOCUS: Analyze the attached file "vector-space.md" verbatim.
TOPIC: Vector Space (Meaning Is a Position)

🎬 SCENE BREAKDOWN:
- SCENE 1: Out of the Layers. (the video's FIRST visual — no title card before it). Sketch a token pill dropping out of a drawn stack of layer slabs, trailing a fresh row of numbers; an arrow points at a small sketched lookup table crossed out with a red X. Write NO text inside the drawing. One black-ink label: "Matches nothing on file."
- SCENE 2: The Taste Test. (HARD CANVAS WIPE). Reproduce the attached illustration "vector-space-1-taste.jpg" — the "Taste Profile · Coke vs. Pepsi vs. Coffee" table — every word and number crisp, held static while narration compares the rows and defines distance. Do not substitute your own visuals; add NO text beyond what it carries; no element of it appears in any other scene.
- SCENE 3: The Payoff. (HARD CANVAS WIPE). Reproduce the attached illustration "vector-space-2-walkthrough.jpg" — the three-card IT walkthrough: ambiguous IT, through the layers, lands by CAT — held static while narration walks the three cards left to right. Same reproduction rules; no element of it appears in any other scene.
- SCENE 4: The Map. (HARD CANVAS WIPE). Reproduce the attached illustration "vector-space-3-map.jpg" — the vector-space map with the word neighborhoods and the path of IT from ambiguous start to landing by "cat" — held static while narration lands "meaning is a position". Same reproduction rules; no element of it appears in any other scene.
- SCENE 5: Not Just Pronouns. (HARD CANVAS WIPE). Reproduce the attached illustration "vector-space-4-airplane.jpg" — the two airplane sentences with their "Nearby on the map" chips — every word crisp, held static. Same reproduction rules; no element of it appears in any other scene.
- SCENE 6: The Close. (HARD CANVAS WIPE). Reproduce the attached illustration "vector-space-5-close.jpg" — the close board — every word crisp, held static. It is the FINAL visual: the last thing on screen, nothing after it.

🗣️ NARRATIVE ARC:
Hook: in the last lesson you watched a token move through the layers, its numbers changing at every step. Out of the layers comes a fresh vector that captures what the token means in THIS sentence. In theory the model could look those finished numbers up in its full table of token embeddings and see which token they match. But it can't: the transformed numbers are one of a kind, lining up with no token in the table. So how does AI work out what the token means, when its vector matches nothing on file? Go back to the Embeddings taste test: Coke, Pepsi, and Coffee, each a row of numbers across seven dimensions. Coke's and Pepsi's rows sit much closer to each other than either sits to Coffee — exactly how the model measures meaning: distance, how far apart two vectors are when you add up the gap in every dimension. The model has established that Coke is closer to Pepsi than it is to Coffee. The same move answers the one-of-a-kind vector: the model doesn't look the numbers up — it reads the token's position. That's vector space: a map where every vector sits somewhere, and similar words share a neighborhood. Now, back to our sentence from the last lesson. On its own, IT is just a pronoun that could mean almost anything, and certainly not CAT. Each layer reads the surrounding words and shifts IT's numbers, and the final numbers land closer to CAT than to any other word in the sentence: the model has figured out that IT means the cat. Meaning is a position: watch IT's journey across the map, past the neighborhoods, until it lands by cat. And it's not just pronouns — every token gets rewritten by the ones around it. Watch airplane twice: folded paper and room pull it toward paper, fold, toy, glide; roared, runway, and sky pull it toward jet, runway, pilot, flight. It isn't one keyword flipping a switch: the final vector is the blend of the whole sentence. Close: "Meaning is a position." over "Close in space is close in meaning." Speak both lines.

Rules:
1. No photographic or stock imagery — no Getty or watermarked images.
2. Show the attached images exactly as provided — never redraw, restyle or replace them.
3. Never put white or light-colored text on a light background, in scenes or transitions; all text is dark ink on light.
4. Required narration: "one of a kind, lining up with no token in the table"; "distance"; "closer to Pepsi than it is to Coffee"; "vector space: a map where every vector sits somewhere, and similar words share a neighborhood"; "the blend of the whole sentence"; and the spoken close "Meaning is a position." then "Close in space is close in meaning."
5. Motion belongs to the drawn scenes: animate builds and transitions in what you draw. On an attached image, move the camera slowly. Hold a board as long as the narration is still walking it, however long that takes; only avoid a frame left up after the narration has passed it.
6. Teach from the visuals, never describe them — never say "this image shows" or "as this graphic illustrates"; speak the idea, let the picture follow.
