Create a video overview of the attached lesson "How AI Answers," about 3 minutes long. The attached how-ai-answers.md is the full lesson text; the numbered images are the lesson's key boxes, in order — show each one as the narration reaches it. Focus: how AI builds its answer one token at a time. Open with the phone's next-word suggestions (image 1) — three choices above the keyboard, pretty much the same for everyone — then the machine the student already built (image 2): the question becomes eight rich vectors, and because attention folds the earlier tokens into the later ones in every layer, the last token's vector carries the entire question. That is the vector the model reads, because the next word goes in exactly one place: right after the last token. Training tuned the layers to turn what the text means into where the next word lives, so that vector lands in the neighborhood of the words most likely to come next: the first word of the answer. Then the heart of the lesson: images 3 through 7 are the SAME answer table growing one row at a time, with the question pinned at the top — show them in order, hold each until its row is narrated, never skip, merge, or reorder them. Each new row is the same move, called prediction: look at the last token, check which neighborhood its vector landed in, take the best-scoring word there. Say the hand-off aloud every time: the word gets typed, joins the context, and becomes the new last token for the next row. The student asked for a name, but the name never came first — the Neighborhood column tells the story: reply starters, ways to suggest, naming verbs, who gets named, and only then dog names. Then image 8 puts the name slot under the microscope: the three predictions per row were just the top of the list — the model assigns a probability to every token in its vocabulary, hundreds of thousands of them, and ranks them all; a neighborhood is just the top of that ranking, and "other tokens" is biggest because the rest of the vocabulary shares that 32%. Name the whole run — tokens to vectors to a ranked list to the next word — inference: what the model does every time it answers you. End on the attached close image, the finished answer — the final thing on screen.
Rules:
1. No photographic or stock imagery of any kind — no Getty or watermarked images.
2. Never show the lesson document itself as a page on screen; show the attached images or draw fresh visuals.
3. Show the attached images exactly as provided — never redraw, restyle, or replace them with your own version.
4. Never place white or light-colored text on a light background; all text and labels must be dark ink on light backgrounds.
5. Do not invent numbers, percentages, or statistics — use only values that appear in the lesson.
6. The narrator never asks the viewer to pause, guess, or answer; answer any question the lesson poses in the same breath.
7. The close image is the final frame of the video — nothing plays after it, no outro or summary scene.
