Create a video overview of the attached lesson "How an LLM Works," about 3.5 minutes long. The attached how-an-llm-works.md is the full lesson text; the numbered images are the lesson's key boxes, in order — show each one as the narration reaches it. Focus: how an LLM gets built and how it answers — it reads more text than a person could in a thousand lifetimes, learns the patterns sitting in that text, turns context into odds for the next word (the same word gets different odds in different sentences), and then writes by predicting one word at a time, feeding each choice back in. Close on the lesson's three warnings: not magic, not a person, not a truth machine. End on the attached close image — the final thing on screen.
Rules:
1. No photographic or stock imagery of any kind — no Getty or watermarked images.
2. Never show the lesson document itself as a page on screen; show the attached images or draw fresh visuals.
3. Do not invent numbers, percentages, or statistics — use only values that appear in the lesson.
4. The narrator never asks the viewer to pause, guess, or answer; answer any question the lesson poses in the same breath.
