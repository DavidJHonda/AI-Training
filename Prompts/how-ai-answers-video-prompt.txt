Create a video overview of the attached lesson "How AI Answers," about 3 minutes long. The attached how-ai-answers.md is the full lesson text; the numbered images are the lesson's key boxes, in order — show each one as the narration reaches it. Focus: how AI builds its answer one word at a time. Open with the phone's next-word chips (image 1), the same for everyone, then the machine the student already built (image 2): the question becomes eight rich vectors, and the last token's vector soaks up the whole question. Then the heart of the lesson: images 3 through 7 are the SAME answer table growing one row at a time — show them in order, hold each until its row is narrated, never skip, merge, or reorder them. Each new row is the same move, called prediction: read the vector on the last token, check which vector-space neighborhood it landed in, take the best-scoring word there. Say the hand-off aloud every time: the pick gets typed, joins the reply, and becomes the new last token for the next row. The name never came first — the sentence built its way to a spot where only a dog name fit. Then image 8 puts step 5 under the microscope: the model scores every token it knows and ranks them all; the three chips were just the top of the list, and "other tokens" is biggest because thousands of words share that 32%. Name the whole run — tokens to vectors to ranked list to next word — inference: what the model does every time it answers you. End on the attached close image, the finished answer — the final thing on screen.
Rules:
1. No photographic or stock imagery of any kind — no Getty or watermarked images.
2. Never show the lesson document itself as a page on screen; show the attached images or draw fresh visuals.
3. Show the attached images exactly as provided — never redraw, restyle, or replace them with your own version.
4. Never place white or light-colored text on a light background; all text and labels must be dark ink on light backgrounds.
5. Do not invent numbers, percentages, or statistics — use only values that appear in the lesson.
6. The narrator never asks the viewer to pause, guess, or answer; answer any question the lesson poses in the same breath.
7. The close image is the final frame of the video — nothing plays after it, no outro or summary scene.
