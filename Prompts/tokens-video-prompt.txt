Create a video lesson from the attached tokens.md. The Markdown is the teaching authority. Follow its order and teach its prose, including the explanations beneath the boards. Complete narration outranks runtime.

REQUIRED VERBATIM AUDIO
Speak every quoted line exactly as written; do not paraphrase, expand, combine, or repunctuate it.

“Math is the magic that powers AI.”
“Instead of giving every word its own number, AI uses reusable pieces of text called tokens.”
“Reuse the pieces. Build more words.”
“Tokenization turns text into token IDs the model can use.”
“A token ID identifies the token. Meaning comes later.”
“Words become numbers.”
“That lets AI work with your language using math.”

TEACH THE COMPLETE LESSON
Open on math powering AI while you ask in words. Give the Avengers exchange, ask how words become numbers, and answer at once. Teach why one number per word breaks down. Define tokens and the vocabulary. Show unbelievable splitting into un, belie, and vable, and un reused in unmatchable and unusual; the letters after un are not one token. Teach where the pieces come from before the chat: the engineers’ choices, the program that builds the vocabulary, and the token ID as an address that says nothing about meaning, used in training and chat. Give the vocabulary sizes as written, no others. Teach the three Send steps by name with unbelievable and its IDs. Teach the cat: instant understanding for you, ID 4719 for AI, not its meaning. Teach all five split examples with pieces and counts, speaking IDs only for unbelievable and cat. SP marks a leading space, not letters the tokenizer adds; a space can join the piece after it. Call the fifth example the web address on the board; never read it aloud. Do not say tokenization ignores spaces or punctuation. Finish the return trip: IDs back into text pieces, joined into the answer you read. Keep every split, ID, and count as written; invent no examples. Do not explain meaning, embeddings, vectors, or neural networks; the lesson stops at the ID. Do not narrate the on-page true-or-false activity.

VOICE
Read the Markdown in its own voice: plain words, short sentences, a teacher talking to one student. Use its sentences as written, adding only brief connective phrases. Avoid words the lesson does not use, such as stakeholders, leverage, framework, utilize, encode, algorithm, subword, semantic, embedding. Never ask the viewer to pause, guess, or answer; supply each answer immediately.

BOARDS AND VISUALS
Show each attached board while teaching its matching content. Board numbers, filenames, and “Teaching content” are production labels, not narration. Keep each board complete, uncropped, and unhighlighted. Board 2 is a text-only version; teach its full content from it. Between boards use simple drawn scenes with no people. No stock photographs, logos, invented facts or statistics, chapter cards, or spoken web addresses.

Open with no title card, welcome, or preview. End with the two closing lines, in order, with a natural statement cadence. Add no summary, sign-off, or words afterward.
