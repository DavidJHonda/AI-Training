Create a video overview of the attached lesson "AI is Different," about 3.5 minutes long. The attached ai-is-different.md is the full lesson text; the numbered images are the lesson's key boxes, in order — show each one as the narration reaches it. Focus, in the lesson's order: AI is built on a different foundation — normal software runs on rules a programmer wrote (the IF-THEN-ELSE box), AI runs on patterns learned once in training, with probability powering every word of every answer; the cooking analogy (a robot following a cookbook vs. a chef who learned from thousands of dishes); the same PS5-game question answered twice — a preset list every time vs. a fresh, tailored answer every ask; what each needs and how each fails (clean rows vs. messy human input; a fixable bug vs. confidently wrong with no line to point to); pick the tool that fits the job; and AI's kryptonite — trained behavior is harder to predict, inspect, and lock down, which is why companies wrap guardrails around it. End on the attached close image — the final thing on screen.
Rules:
1. No photographic or stock imagery of any kind — no Getty or watermarked images.
2. Never show the lesson document itself as a page on screen; show the attached images or draw fresh visuals.
3. Show the attached images exactly as provided — never redraw, restyle, or replace them with your own version.
4. Never place white or light-colored text on a light background; all text and labels must be dark ink on light backgrounds.
5. Do not invent numbers, percentages, or statistics — use only values that appear in the lesson.
6. The narrator never asks the viewer to pause, guess, or answer; answer any question the lesson poses in the same breath.
7. The close image is the final frame of the video — nothing plays after it, no outro or summary scene.
